/**
 * Programmatic entry point for the timone library.
 *
 * The CLI lives in ./cli.ts; this module re-exports the public API for
 * consumers that embed timone programmatically. Nothing is exported yet —
 * exports are added as features land in later phases.
 */
export {};
