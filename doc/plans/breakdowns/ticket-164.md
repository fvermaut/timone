# Breakdown

**Status:** Approved by fvermaut 2026-09-26 — 2 pieces. Given in the terminal session `e9bd67c9-0d4c-4797-8329-7a5bfe529e75`: these two pieces were proposed, and he answered *"look I understand, it's more complex than I thought, do as you think it's best"*.

1. **The runner, beside the current daemon** ([#165](https://github.com/fvermaut/timone/issues/165)) — an agent, the runner, decides each step of a run on a project switched to it, through the actions the machine gives it. The machine keeps the run record, lists every departure on the pull request, holds each ticket to $150, passes the runner only named people's comments, wakes it on events and every 15 minutes, lets it message or stop a running step, and starts it again when it fails. The replay of the recorded failures passes. scratch-app is switched to the runner, and ivtrends stays on the current daemon, where `timone retry` still works. Delivers PRD-05 R1 to R19, except the removal of `timone retry` in R11.
2. **The old code between steps is removed** ([#166](https://github.com/fvermaut/timone/issues/166)) — once ivtrends has also been switched to the runner and has run a ticket on it, the code that picks the next step, reads a step's end from an exact line and decides where a run waits is deleted, and `timone retry` with it. `process.md`, the step skills and the glossary describe the runner, and the decision records ADR-0060 replaces are marked as replaced. Delivers PRD-05 R20 and the rest of R11.

**Why two and not one:** the old code can only be deleted after ivtrends runs on the runner, and ivtrends can only run on the runner after the first piece is merged. **Why not more:** the first piece could be cut into smaller pieces that each work on their own, but each would be one more pull request to review, and fewer pull requests is what fvermaut asked for. The cost is that the first pull request is large.
