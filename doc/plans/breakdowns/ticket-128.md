# Breakdown

**Status:** Awaiting approval

1. **One short question at the approval gate** — a reply that is neither a yes nor a change request gets one short question instead of a demand for a terminal session, and answering it closes the gate. Carries the ask check itself: two outcomes, no way to reach a gate outcome or a run's state, one question per ask.
2. **The same question everywhere else a person is asked** — the check moves from the approval gate to every message that asks a person for something, and the question it writes is held to the project's writing rules.
3. **A dead stop unsticks in writing** — when the check has framed a question on a run the machine gave up on, answering it in writing starts the session that a terminal command starts today.
