# Handover — Timone — 2026-08-14

> Prior handover: [2026-08-11-phase-19-approved.md](2026-08-11-phase-19-approved.md). Its "Exact next action" — *execute phase 19* — was done by this session, along with phase 20.

## Snapshot

**[Phase 19](../plans/phases/phase-19.md) and [phase 20](../plans/phases/phase-20.md) are both Complete**, built, gated and closed in one session. `main` is at `df453ef`, clean; **792 tests across 23 files**, type-check clean. Neither phase went as planned: **19 cost an ADR because its approved design was unbuildable**, then a second amendment because its live gate failed; **20 executed as approved but its gate found ten findings**, three of which no test could have seen. **[ADR-0026](../adr/0026-a-ticket-is-a-conversation-a-run-is-a-chunk.md) is decided and entirely unbuilt** — it separates a ticket from a run, which is the deepest change to the ledger since the daemon existed. `PRD-02` stands at **17 of 21 verified**: R3 moved to `verified`, R21 is **`draft` at six clauses of eight**, R20 stays `failed` with clause 2 now built.

## Done this session

- **[Phase 19 closed](../plans/phases/reports/phase-19-complete.md)** — six slices, `9f688e7`…`2278a0c`, closed at `48299a8`. One writer on the ledger, the claim written before the session exists, an answer consumed when read, one thread fetch per parked run, and 19g added mid-phase.
- **[Phase 19's live gate](../plans/phases/reports/phase-19-live-gate.md)** — all five steps, ~$6. **R3 → `verified` on fvermaut's own judgement of answers he wrote himself**, not on the diff.
- **[Phase 20 closed](../plans/phases/reports/phase-20-complete.md)** — seven slices, `704180f`…`2c931d3`, closed at `e93a8b7`.
- **[Phase 20's live gate](../plans/phases/reports/phase-20-live-gate.md)** — **ten findings**, and the fault the phase existed for is dead: a map closed its questions, said so, and moved when fvermaut told it to.
- **[ADR-0025](../adr/0025-a-lock-holders-proof-of-life-is-its-process.md)**, **[ADR-0026](../adr/0026-a-ticket-is-a-conversation-a-run-is-a-chunk.md)**, and register markers on R1/R20/R21 (`2a2fdd4`).
- **Fixture pollution reverted on `scratch-app` twice** — `2d80e64` and `af01893` — and the ledger residue cleared (`f12cf75`).

## In flight / blocked

- **Nothing is half-built.** Both phases are closed and the tree is clean.
- **ADR-0026 is decided and unbuilt.** It creates one concrete new capability the machine has never had: **planning a specification into chunks, proposed for a single approval.** Today one ticket yields one phase file.
- **R21 cannot be verified as it stands.** Clause 3 is unobservable because fvermaut enabled `introduce_unmarked` on **both** projects (`7693de8`), so no project is in the default state; clause 6's session-opening half was never seen. The ten-minute idle write check **stands at 4m19s** — zero writes over that window, cut short by his approval and then by a GitHub outage.
- **fvermaut's daemon is stopped and must be restarted.** A running daemon keeps the code it started with, so **neither phase is live on his machine** until he does. This is how the session began: the gate's first hour ran against pre-phase-19 code.

## Decisions made this session

- **[ADR-0025](../adr/0025-a-lock-holders-proof-of-life-is-its-process.md) — a lock holder's proof of life is its process.** Forced by execution: [ADR-0023](../adr/0023-one-answer-one-session.md)'s reclaim clause **could not be built**, because `RunStore.witness` ends in `persist()` — consulting ADR-0020's witness *writes to the file the lock protects*. Reproduced at real defaults: three refused `daemon --once` starts a minute apart broke a **live** daemon's lock.
- **[ADR-0026](../adr/0026-a-ticket-is-a-conversation-a-run-is-a-chunk.md) — four rulings**, put one at a time after the gate showed an approved map walking into building. It **discharges [ADR-0015](../adr/0015-branch-per-driving-unit.md)'s own deferred question**, which named itself as the place this would be taken up, and answers it against its default.
- **Phase 19 amended twice, re-approved twice** — `c846f20`/`2c7f04e` (ADR-0025) and `b011d68`/`8bf0119` (19g, after the gate failed step 3).
- **Phase 20's plan has no documentation sub-phase** where 19 had 19f. Handled as phase-close work and recorded as a deviation; **the next Timone-self plan should carry one explicitly.**
- **Three orchestrator file-grant widenings**, all recorded in the handoffs: phase 20 carries **no file markers for any sub-phase**, so the narrow grants were the orchestrator's construction rather than the plan's, and correcting them was not a plan amendment.
- **fvermaut declined to turn one question into a rule.** Whether work discovered mid-build is added silently or stopped for *"depends what was found"* — and the real answer is a short conversation at the moment it happens, in the **chat channel that has been next to build since 2 August**. Recorded in [STATUS.md](../../STATUS.md), deliberately not decided.

## Exact next action

**Two things, neither blocking the other.**

1. **fvermaut restarts his daemon** — `timone daemon`. Nothing else makes phases 19 and 20 real on his machine.
2. **Stage 7 on phase 20** — `/timone-verify` is not applicable (Timone is not a managed project), so this is a fresh-context pass over [phase 20's criteria](../specs/prd/prd-02-inversion-of-control.criteria.md#r21--every-open-ticket-answers-for-itself) by an agent that did not build it. **It must settle R21's own internal contradiction first**, because clause 1 cannot be checked against a rule nobody has stated: **clause 1 promises every open ticket carries a line; clause 3 blesses silence on a backlog project.** Three separate slices flagged it independently and it is fvermaut's to settle.

**Do not start building ADR-0026** until the four questions it leaves open are settled — and note that fvermaut stopped the grill on those as out of current scope.

## Open questions

- **R21's clause 1 versus clause 3** — every open ticket carries a line, against a backlog project staying silent. **Resolved by:** fvermaut. It blocks verification, not just delivery.
- **Four questions ADR-0026 leaves open:** when a ticket closes; what its thread says between chunks; whether a chunk's PR review lands on the ticket or stays on the PR; how existing runs migrate (every one is already a ticket with one chunk, so they are conformant). **Resolved by:** fvermaut, when the chunking is scoped — he judged grilling them now out of scope.
- **The uncommitted-change blind spot** — the guardrail published a false accusation against an innocent session, because [phase 15](../plans/phases/phase-15.md)'s fix reads provenance off commits and an uncommitted change carries none. Finding 1 of the phase-20 gate. **Resolved by:** whoever owns the next correctness pass; it is a live public-comment risk.
- **A queued run promotes onto a closed ticket and starts a session on it** (finding 8), and **there is no way to end a run at all** (finding 9) — clearing residue needs hand-editing the ledger, which [phase 13](../plans/phases/phase-13.md) recorded as fixed. They close together.
- **A map's frontier is a label, not a fact** (finding 10) — `ivtrends` #1 has every question closed and would still report itself as working. The only finding on a live project. **Manual route out:** add `timone` and `wayfinder:frontier-empty` together.
- **The frozen output-token counter** — R17's remainder, seen live again: static across four heartbeats, then correct at the close. It **lags rather than dies**. **Resolved by:** an investigation nobody has scheduled.
- **`timone status` still cannot see blocking**, carried unresolved from the last three handovers.
