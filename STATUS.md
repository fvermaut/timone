# Timone — Status

**For fvermaut. Plain language, no process knowledge assumed.** This file is a status report, not agent context — agents update it, they never treat it as a source of truth. Last updated **2026-08-15** (**a big job is now agreed once as a list of pieces and arrives a piece at a time**, each as its own pull request — and the plan for each piece is something you can read rather than something you have to answer. Details in the first section. **Nobody has watched it run yet**, and the count of kept promises went *down* today as a result: two of them describe a machine that no longer exists. **Earlier: the thing you did on 13 August that produced nothing now works** — a list of questions that finishes now tells you so, and when you say go ahead on it, it writes the specification by itself. You watched that happen and approved the result yourself, on the ticket. Details in the second section. **Earlier the same day: the fault that answered you twice is fixed, and you are the one who proved it** — you wrote answers on the throwaway app in your own words and judged what came back, and **you can write your answers on the trading app's questions again**, with one condition: restart the watcher first, or you are still running the old code).

~~**Earlier, and still true — the independent check ran, and it did not pass.**~~ **Half of it is fixed as of 2026-08-14.** The write-your-answer-back machinery ~~works — but it answers you **twice**, from two separate sessions, and that reproduced on both attempts~~ **now answers you once, watched doing so on your own written answers.** ~~**Do not answer the trading-app questions in writing until this is fixed.**~~ **That instruction is lifted** — see the next section for the one condition attached to it. **The second problem is still open:** one written promise about those tickets is something the machine deliberately does not do. You ruled on it on 2026-08-13 — build the missing path — and that build is the next one, not this one. Since 2026-08-11 you answered and closed **five** questions on the trading app by talking them through, and six new ones are open.

> **This file is about Timone itself** — the tool. Each managed project has its own `STATUS.md` file; the to-do app's is at `projects/scratch-app/STATUS.md`. **Every file path in this file lives in the Timone repository unless it says otherwise.** When something here is about a project, it names the project and says so.

---

## In one paragraph

Timone makes AI agents follow a real engineering process instead of improvising. The process runs in **stages** — sort the request, dig out requirements, write them down, record decisions, plan, build, check, deliver, act on feedback. Each stage is driven by a **skill**: a written instruction file an agent loads and follows, the way a new colleague would follow a runbook. There are twelve stages in all, and **ten of them now have a working skill**. The two that don't are deliberately left for later. The real gap — the stage that handles your feedback — was closed on 2026-08-02, and later the same day it was watched going the whole way round: a review finding on the to-do app became a change to that app's requirements, a plan, code, an independent check and a new pull request waiting for you. **The process is now a loop rather than a line** — it no longer ends when work is delivered. After that comes the bigger goal: having all this run automatically from tickets instead of being typed by hand — and **as of 2026-08-03 the first piece of that runs for real.** A ticket filed in plain language on the to-do app was noticed, acknowledged, sorted and answered without anyone typing an instruction. **As of 2026-08-05 it goes further**: it interviews you when it needs to, writes down what you asked for, plans it, and asks your permission at each of those two points — stopping only where the building itself would start, because that is the next thing to build.

## What "done" means here

Timone's own requirements are written down as a numbered list in `doc/specs/prd/prd-01-process-layer.criteria.md` — one file, twenty-four entries, each stating what must be true and how to check it. **A requirement counts as verified only when an agent that did not build the thing has checked it and recorded evidence.** That is Timone's *process* half. The *automatic* half has its own list at `doc/specs/prd/prd-02-inversion-of-control.criteria.md`, now ~~**twenty entries, of which sixteen are verified**~~ ~~**twenty-one entries, of which seventeen are verified**~~ ~~**twenty-one entries, of which sixteen are verified**~~ **twenty-two entries, of which fourteen are verified** — the twentieth was added on 2026-08-09 for the map-question work, the twenty-first on 2026-08-13 for making every open ticket say what happens next, and the twenty-second on 2026-08-15, for a ticket that carries a whole run of pieces instead of one. ~~**Four are outstanding.**~~ ~~**Five are outstanding as of 2026-08-14**~~ **Eight are outstanding as of 2026-08-15.**

**Read that again, because the number of kept promises went *down* today — sixteen to fourteen — and a count that quietly went up would be a lie.** Nothing broke. **Two promises lost their tick because what they promise changed**, and both changes are ones you asked for on 14 August: the promise that you are asked to approve each piece's plan (you are not asked any more — you approve the list of pieces once instead, described in the next section), and the promise that a project only takes on the next ticket when the current one has finished (it now frees up **between pieces**, which is the whole point of the change). Their old sign-offs describe a machine that no longer exists, so they stop counting until somebody who did not build the new one watches it work. **A third is simply new**: the twenty-second entry, written on 15 August, has never been checked by anybody. The fourth of the eight ~~is there by your own decision rather than by a failure~~ **is the safety-check one, and it is the same story from 14 August**: you changed where the safety checks report, which changes what that promise says, so its old evidence stops counting until somebody re-watches it. That rule — change the wording, lose the sign-off — is the one this file has always claimed, and it has now been applied to Timone itself **three times**. Two are long-standing: the live counter of how much a job has written, still wrong and still unexplained; and "your review comment gets acted on *and* the preview updates", one clause short and closing free on the next real ticket that gets that far. ~~**The other two are new and are failures rather than gaps** — the write-your-answer-back promise and the map-questions promise both went to `failed` at the independent check.~~ **On 2026-08-14 the write-your-answer-back promise passed** — on your own answers, which is what it had always been waiting for. **The map-questions promise is still a failure**, and deliberately so: the thing that answered you twice was one of three faults counted against it, and the other two are untouched. ~~The larger of them is the run-this-line command refusing a question it has never seen, which you ruled on and which the next build fixes.~~ **Built on 2026-08-14** — that command now picks up any open ticket by reading the ticket itself, and the refusal you hit in August is gone from every open one. **The promise still counts as a failure**, because a promise counts as kept only once somebody who did not build it has checked it, and one part of it has still never been seen at all: two of the four kinds of map question have never existed since the work was done. ~~The twenty-first is not started.~~ **Built in full on 2026-08-14 — and deliberately not marked as kept.** Six of its eight parts were watched working, two were not, and the honest remainder is in the next section. **22 of the process list's 24 are verified** — you signed off the feedback stage on 2026-08-02, and the four before it on 2026-07-29 (the delivery skill, the two-review mechanism, the handover writer and this status file).

**The feedback requirement was signed off with two limits written into the record rather than left implied**, because a sign-off that hides what it did not prove is worth less than one that names it. First, that requirement lists **six** kinds of response and there are now **seven** — the wording is out of date, and correcting it is the feedback stage's own job on its own record, not something a checking pass may do. Second, one rule inside it has a live example that **does not actually prove the rule**: when you change a requirement, its old evidence stops counting and it drops out of the safety net — and that is exactly what happened to the to-do app's on-screen behaviour rule, except it would have dropped out anyway for an unrelated reason, so the rule was never the thing doing the work. Proving it properly needs a different requirement to change, and none has yet.

## Just finished — you agree the pieces once, and they arrive one at a time

**2026-08-15.** This is the change you settled on 14 August, and it is now built. **Nobody has watched it run yet** — see the honest remainder at the end of this section, which is longer than usual.

**What is different, in one paragraph.** A big piece of work used to be one ticket, one branch, one enormous pull request, and two separate moments where you had to say yes. Now: you approve the specification as before, and then the machine comes back with **a list of the pieces it proposes to build** — plain sentences, one per piece, posted on the ticket and written into a file so it is a document and not a chat message. **You say yes once, to the list.** After that each piece is planned, built, checked and delivered on its own, and lands as **its own pull request** — small enough to actually read. When you merge one, the next starts by itself. The ticket closes when the last one merges, with a comment linking every pull request the whole thing produced.

**Three things you can do this morning that you could not yesterday.**

- **Judge the shape of the work before any of it is built.** *"Is this the right list of pieces?"* is the one question here that is genuinely yours, and it is now the only one you are asked. Approving the list also puts the agreed specification onto the project's main line straight away, so the first piece is built against it rather than against a stale copy.
- **Read the plan for a piece instead of approving it.** Each piece still gets a written plan, still committed where you can read it, and the machine still posts what it intends to build in plain words — **but it asks you for nothing.** You agreed to the piece when you agreed to the list; the judgement it still owes you lands on its pull request, with the code in front of you. That is one fewer interruption per piece, and on a five-piece job it is five fewer.
- **File a bug in the middle of a big job and have it looked at in days, not weeks.** A project used to be held by the *ticket*, so everything else queued behind a milestone until the whole milestone finished. It is now held only by **the piece being built**, so between one piece merging and the next starting the project is free, and whatever is waiting takes its turn there.

**Three smaller things, each of which you would have noticed if they were missing.**

- **The ticket says where it stands, by itself, every minute.** *"Building piece 2 of 3."* while one runs, *"Piece 2 of 3 is next."* between them, and the review request now names which piece it is. A ticket with no pieces reads exactly as it always did — that was checked word for word, because nearly every ticket you have is that kind.
- **If the list grows after you approved it, everything stops and you are asked again.** A job that discovers it needs a fourth piece does not quietly build a fourth piece; the ticket says the list has grown since you approved it and waits for you. It will not close the ticket and it will not start the extra work.
- **A small chore now goes straight to a pull request with no approval step at all.** You chose this on 15 August, having been shown the risk in the same breath: **nothing stops a misread chore before the work happens.** You accepted that, on the grounds that merging is still your act. This is a check you *gave up on purpose* — it is not a bug, and it should not be "fixed" by somebody later.

**The honest remainder, and it is the whole reason two promises lost their tick above.**

- **None of this has been watched working.** 927 automatic checks pass across the machine's own test suite, and **that is all**. No real ticket has been through it, no list of pieces has ever been put in front of you, and no second piece has ever started by itself outside a test. The proof run on the throwaway app is the next thing, and it is where this either holds up or does not.
- **Nobody independent has checked it.** A promise counts as kept only when an agent that did not build the thing watches it and records evidence. That has not happened for any of this.
- **The refusal to build an unapproved list is written down, not enforced.** The rule that a piece from a list you never approved must not be built lives in the instructions a building session reads and obeys. **The machine itself does not check it** before starting. It is the difference between a rule and a lock, and today it is a rule.
- **`timone status` says a project is idle between pieces.** The ticket says *"piece 2 of 3 is next"*; the terminal says nothing at all about it. The two do not contradict each other — both mean "nothing needed from you" — but the terminal is the poorer of the two, and closing that gap was deliberately left out of this build.
- **Two pieces of the same ticket look identical in the project's history.** Every commit says which ticket it came from and none of them says which piece, so the record cannot yet tell piece 2 from piece 3. Known, written down, and nobody's current job.

**Where the detail is:** `doc/adr/0028-…`, `doc/adr/0029-…` and `doc/adr/0030-…` in this repository (the decisions), `doc/plans/phases/phase-23.md` (what was built), and `doc/plans/phases/reports/phase-23-handoffs.md` (how, step by step, including four places where the plan turned out to be wrong and was corrected during the build).

## Just finished — the thing you did on 13 August now works

**2026-08-14.** On 13 August the trading app's list of questions finished, and you wrote on it: *"ok go ahead and write the spec."* **Nothing happened, and nothing was going to.** That is fixed, and it was watched working end to end on the throwaway app with you at the keyboard.

**What now happens instead.** A list of questions is an ordinary ticket like any other. While its questions are still open it says so — *"nothing right now — I'll come back here when the last one is closed"* — and it stays put whatever anyone writes on it. When the last question closes, **it changes its own line, by itself, within a minute**, to *"say go ahead here and I'll write the specification this map has been finding its way to."* You reply in your own words, and it writes the specification, on its own branch, and comes back and asks you to approve it.

**You did all of that, and it is the part that counts.** You typed the go-ahead. A session started on the list's own job, spent 17 minutes and 30 seconds and $2.55 writing the specification, and stopped to ask you. **You replied `approve` on the ticket and it read your answer** — the identical gesture that had produced absolutely nothing the day before.

**Three other things you can see on any ticket now.**

- **Every open ticket says what happens next.** All five open on the throwaway app carried a line: one says the next step has never been built and nothing can move it, one hands you the exact command to restart a job that stopped, one is waiting on your answer, and two tell you the label that would hand them over. Each is checked and repaired **every minute** rather than written once and left to rot.
- **What the machine tells you in the terminal and what the ticket says are now the same sentence** — word for word, because there is only one place deciding it. That was the fault where `timone status` asked you to answer trading-app #11 while #11 said nothing was needed.
- **A ticket you file without the label gets one comment introducing the machine**, saying that only labelled tickets are picked up and that the label is what hands it over. **Exactly one, ever** — and no work is ever started on it. **You turned this on for both projects** on 14 August, the throwaway app and the trading app. On the trading app that produced exactly one comment, on the questions list.

**What it cost:** about **$6** of sessions across the whole proof run, on the throwaway app.

**The honest remainder, and there is more of it than usual.**

- **Two of the eight promises were not proven, so the requirement is not marked as kept.** The first is the promise that a repository joined with a big pile of existing tickets stays silent — the switch that does that defaults to off, and **you turned it on for both projects before the run**, so there is no project on this machine in the default state to watch. It is proven in the automated checks and not in the world. The second is smaller: taking over a ticket the machine had never heard of **did create the job**, which is the point, but the conversation did not open because the project was busy with the list — so half of that one is unwatched.
- **The ten-minute quiet check stands at four minutes.** The promise is that a machine with nothing to do writes nothing at all. It wrote nothing for **4 minutes 19 seconds** — and then you typed `approve`, which ended the quiet. The retry was defeated by GitHub itself going down. Four minutes of silence is what was measured; ten is still owed.
- **The safety check publicly accused an innocent session.** It posted on a ticket that the session had changed a file outside the project. **It had not — I had**, minutes earlier, and had not committed it. This is the same fault that was fixed in August, meeting the one gap the fix does not cover: the fix works by reading a label every commit carries, and **an uncommitted change carries no label**, so nothing can tell whose it is. It is written on a ticket where anyone can read it.
- ~~**A finished list of questions now walks straight on into building, and nobody decided it should.** Once you approved the specification, the same job carried on into planning and took a branch — left running, it would have built the whole feature. That follows correctly from what you approved, but **whether a list of questions should *become* the thing that gets built, or hand over to a fresh ticket, was never put to you.** I stopped it rather than spend a build on a throwaway. **It is listed under "Waiting on you".**~~ **You settled this on 2026-08-14, and it went further than the question asked.** A ticket becomes **a conversation that lasts**, and the work under it becomes **chunks** — each one built on its own and shown to you as its own pull request. So a list of questions doesn't *become* the build; it *carries* it, the way an email thread carries a subject. Three consequences you chose: only the chunk being built holds the project, so **a bug you file mid-way waits days rather than weeks**; you approve the shape of the work **once** rather than approving every chunk's plan; and there is no such thing as a special "big ticket" — a bug is a ticket with one chunk, a milestone is a ticket with five, and *"the page feels slow"* can turn out to need three without anyone reclassifying it. Written down as `doc/adr/0026-a-ticket-is-a-conversation-a-run-is-a-chunk.md`. ~~**Nothing is built yet**~~ **Built on 2026-08-15 — see the top of this file**, and it is the deepest change to the machine's bookkeeping since the watcher existed.

- **One question underneath that you deliberately did not turn into a rule, and you were right not to.** If building one chunk uncovers work nobody knew about, does the machine quietly add it, or stop and ask? Your answer was *it depends what was found* — plumbing the work plainly needs is one thing, a change to what the product promises is another. But you also said the real answer is that **the machine should ask you, in a short conversation, at the moment it happens** — which is the chat channel (Slack or Teams) that has been the next thing to build since 2 August and still doesn't exist. **That is worth noticing as an argument for building it:** a fixed rule is what you settle for when interrupting you is expensive. Make interrupting cheap and the rule stops being needed.
- **A ticket that calls itself a throwaway had its decision written into the app's permanent records again** — second time in two days, undone again. One occurrence is an accident; two is a mechanism.

**Where the detail is:** `doc/plans/phases/reports/phase-20-live-gate.md` (what was watched, with timestamps, and all eleven findings — four of them added after the run finished, as its leftovers were cleared and then as that clearing was itself checked) and `doc/plans/phases/reports/phase-20-complete.md` (what was built).

## Just finished — you get answered once, and you proved it yourself

**2026-08-14.** The fault at the top of this file since 11 August — you write one answer and two of the machine reply — is fixed, built and watched working. **It was proven with your answers, not with answers a machine typed pretending to be you**, which is the part that had never happened before.

**What was wrong, in plain terms.** Nothing stopped two watchers running at once. The machine put its name on a job only *after* starting the work, so for minutes the record said "nobody is on this" while a session was already running and being paid for. The check that should have noticed was reading a stale copy in its own memory rather than the file on disk. And a fourth thing needed no second watcher at all: a session that answered you without posting anything left its bookmark sitting *before* your comment, so the next minute it read your answer again as though it were new.

**What changed.** One watcher at a time, enforced rather than assumed — a second one stops and tells you which one is already working, by name. A job is claimed *before* the work starts, and the claim is given back if the work never starts. The check reads the file. And reading your answer moves the bookmark past it, so nothing reads it twice. **Your comment is never touched** — it stays on the ticket, word for word; only the machine's bookmark moves.

**What you did, and what you judged.** You wrote answers on the to-do app's throwaway questions in your own words — one of them answered twice, on purpose — and said whether what came back was a fair reading. Three judgements:

- **Five ambiguous words** — *"just do it automatically"*. You called the reply **fair, and said it flagged the ambiguity well**: it made the call, gave its reasons, said out loud which half it had decided on your behalf, and said what would flip if you had meant the other thing.
- **A deliberately partial answer**, on a different question. You called the single follow-up **reasonable, and said it held what you'd settled**: it did not re-ask the part you had answered, asked exactly one question, went and looked at the app instead of asking again, recommended an answer, and told you one word would do.
- **A deliberately contradictory answer**, written straight after that one. You called it **a pass** — *"it didn't badger, and the reading is good"*. **And this one is worth your attention, because it did not do what the rulebook says.** The rule is that a second unsettleable answer makes the machine stop and hand the conversation back to your terminal. It didn't hand back; it worked the contradiction out instead, said what it was deciding for you, and closed. **The point of the rule held — you were never asked a third time — but the rule as written describes a different move.** You judged the outcome good. Whether the rulebook or the behaviour changes is a decision still to be made, and it is written down rather than tidied away.

**One condition on lifting the restriction, and it is not optional.** **A watcher that is already running keeps running the code it started with.** During the proof run, a watcher that had been up since the afternoon answered a ticket using the *old* code, cheerfully, with the fix sitting on disk beside it. So: **stop and restart the watcher before you write your next answer on a trading-app question.** Until you do, nothing above is true of your machine.

~~**Talk the trading-app questions through rather than writing your answer on the ticket.**~~ **Lifted 2026-08-14** on the evidence above. Write them if you prefer; run the line the ticket gives you if you prefer that. Both work, and both now happen once.

**What it cost and what it found.** The whole proof run was about **$6** on the throwaway app. It found **six** things 695 automatic checks could not, and the two that matter to you are already above (the restart, and the rulebook disagreeing with the behaviour). Of the rest: the machine's own log prints a "resuming" line *after* the session has already finished, which looks exactly like the bug that was just fixed and will confuse the next diagnosis; a ticket that says in its own words "this is a throwaway, not a real decision" **still had its decision written into the to-do app's permanent records** — undone on your call, and worth knowing because it is how "it's only the test app" quietly becomes real mess; a watcher that is killed outright leaves its project locked for up to two minutes before another can take over, which is a deliberate trade rather than a fault; and a question waiting on you still opens by promising a write-up it is forbidden to produce, now noticed for the third time. The sixth is housekeeping — the one test that fails now and then, unexplained since 11 August, was identified and is not caused by any of this. **All six are listed under "Known problems" at the bottom, and none of them is fixed.**

**And one honest failure on the way, which is why this took two days.** The first proof run failed the third of its five checks. Kill a session halfway through reading your answer and the recovery command was supposed to hand the answer back — instead it **asked you the same question again, over the top of what you had written**, and your words were never read. That is worse than the problem being fixed, so the work was stopped, the plan changed, an extra piece built, and the check re-run. It now winds back to just before your answer, says so in plain words, and reads it. No second copy of the question lands on the ticket.

**Where the detail is:** `doc/plans/phases/reports/phase-19-live-gate.md` (what was watched, with timestamps) and `doc/plans/phases/reports/phase-19-complete.md` (what was built).

## Before that — the tickets that don't tell you what to do next

**2026-08-13.** The trading app's list of open questions finished — fifteen asked, fifteen answered — and the machine wrote a summary saying the decisions would now become a written specification. You replied on that list: *"ok go ahead and write the spec."* **Nothing happened, and nothing was going to.**

**Two separate reasons, either one enough on its own.** The list itself was deliberately built to be invisible to the machine — it is an index of the effort, not a question, so nothing watches it. And even if something had, there was nowhere for your instruction to go: the step where a finished list of decisions becomes a written specification is **the only handover in the whole process with no way in from a ticket**. It has always required you to type a command.

**Then it got bigger.** Every open ticket across both projects was checked the same day. **All four fail**, each differently:

- **The trading app's question list** — says "nothing right now", invisible to the machine, and the command that would take it over refuses.
- **"the page feels slow"** on the to-do app — sitting at a stage whose next step has never been built. Its ticket says so honestly, and nothing can move it.
- **"can I get a dark mode?"** — you filed it on 3 August without the label. It has been silent ever since, and nothing on it explains why.
- **"I can't tell at a glance how much is left"** — its job stopped early. `timone status` tells you the one line that would restart it; **the ticket itself doesn't**.

And two faults already written down here turn out to be the same disease: the machine telling you to *"answer on ivtrends #11"* while #11 says nothing is needed, and the fact that nothing forces it to go back and update tickets that were waiting on something once that something is settled. Both are a next-step line that was true when written and quietly went stale.

**You made six rulings.** Each was put to you on its own with what it would cost. In short: **every open ticket must say what happens next**, and where something can move it, name that thing exactly — and where nothing can, say so and say what would unblock it. That covers **every** open ticket, including ones you never handed over: those get introduced to **once**, so nothing you file sits silent again. The question list becomes a proper ticket you can answer. The takeover command stops refusing tickets it has never seen. And the next-step line gets **repaired every minute** rather than written once and left to rot.

**Two consequences you weren't asked about, because they only became visible afterwards.** Once you say "go" on a question list, that list holds its whole project until the specification is written — no other ticket on that project moves meanwhile. And this **cannot be built before the duplicate-answer fix** that is already approved and waiting: the repair runs over exactly the machinery that answers you twice, so building it first would spread that fault from one ticket to every open one, every minute.

**Written down, planned, and approved the same day.** The reasoning is `doc/adr/0024-every-open-ticket-answers-for-itself.md`, the requirement is R21 in the list at `doc/specs/prd/prd-02-inversion-of-control.criteria.md`, and the work is `doc/plans/phases/phase-20.md` — ~~**eight steps, approved 2026-08-13, and you are running the build in another session.**~~ **All eight steps are built and proven, on 2026-08-14** — see the top of this file. As with everything else, the live proof ran on the throwaway to-do app, not on the trading app.

**All four of the failing tickets above are answered by the build**, and three of the four were re-read at the proof run and carry a proper line now. The fourth is the trading app's question list, which was not the proof and is not claimed: **it benefits as ordinary use from here, and the first thing that happens to it is that it starts saying what it needs.**

~~**Two builds are now queued, and the order matters.**~~ ~~**The first of the two is done.**~~ **Both are done, on 2026-08-14.** Phase 19 — the duplicate-answer fix, approved on 11 August — had to be built and proven **first**, and it was; phase 20 followed it the same day, once its precondition was met. ~~**Until both are done, the instruction below still stands: talk the trading-app questions through rather than writing your answer on the ticket.**~~ **Lifted 2026-08-14, with one condition: restart the watcher first.**

**One thing this does not fix, and it is worth knowing.** The trading app's specification can still be written today — by talking it through in a session, the way everything else has been. What does not exist until this is built is your ability to have said so **on the ticket**, which is what you actually did.

## Before that — the independent check, and it found two real problems

**2026-08-11.** The write-your-answer-back work was checked by an agent that did not build it. **It did not pass**, and both findings are worth your time. Full write-up: `doc/plans/phases/reports/phase-18-verification.md`.

**Problem one: one answer from you sets two of the machine going.** You write your answer on a ticket. Two separate sessions pick it up about thirty seconds apart, both work the question out from scratch, both write a full answer back, and both close the ticket. You get told the same thing twice, in two differently-worded messages, and the machine's own log ends on an error. **This was reproduced on both attempts** — it is not a fluke.

Both times, the two answers happened to agree. **That is luck, not design.** On a real question the two could reach different conclusions and write both down as settled, and you would be left to work out which one counts. It also pays for two sessions instead of one — about a dollar each, on the throwaway app.

The machine caught itself doing it, which is the only pleasant part: on the second run a session posted a comment explaining that two of it had answered the same question, and that comment was left on the ticket rather than tidied away so it stays findable.

~~**This is why the six trading-app questions must wait.** Answer one in writing today and it gets answered twice, on a real product decision.~~ **Fixed and proven 2026-08-14** — see the top of this file. **Talking them through was never affected** — that path is a different mechanism, and it is the one you have been using.

**Problem two: a written promise the machine deliberately doesn't keep.** The requirement says that if a map question hasn't been connected to the machine yet, the run-this-line command should still work on it by reading the ticket. It doesn't — it refuses, with the same sentence this whole piece of work existed to get rid of. That refusal is not a bug someone missed; it is a design decision taken during the build, which chose to connect tickets up front instead. **So either the requirement is worded around a mechanism and should be reworded, or the missing path is genuinely owed.** That is a call for you, not something a fixing session should guess at.

It matters in practice for one case: a repository that was set up before this work existed has no `timone` label at all, and connecting a ticket there **fails silently**. Then the command refuses and there is no other way in.

**What the check did confirm, so this is not all bad news.** Your answer *is* picked up on its own, within a minute, with nothing typed by hand — watched twice. The one-follow-up-then-hand-back limit was watched working properly on the throwaway app. The five trading-app questions you talked through all landed proper written records. And the everyday plumbing was re-checked and still holds: a labelled ticket gets picked up and ~~an unlabelled one is left completely alone~~, and nothing of the machine's own files has leaked into either project. ✏ **The middle one changed on 2026-08-13's ruling and was built and watched on 2026-08-14** — an unlabelled ticket now gets exactly one comment introducing the machine, so nothing you file sits silent. It still never gets *worked on* without the label — no job is ever created for it, which was re-checked live — and that is the half the rule was really about.

**One thing the check could not do, and says so.** Five of the ten safety-net requirements it should have re-run — the approval gates, building, the pull request, previews — were not re-run, because doing so means a full ticket-to-pull-request cycle with Docker and several paid sessions, which is out of proportion to work that only touched the answering path. That gap is named rather than implied.

~~**And the honest limit that has not moved.** Every written answer the machine has ever been tested against was typed by a machine playing you. Whether a *real person* would agree it gave up at the right moment is still unwitnessed.~~ **Mostly closed on 2026-08-14, and the remainder is worth naming.** You wrote the answers yourself and judged the replies fair. What is *still* unwitnessed is the giving-up itself: on the contradictory answer the machine worked the contradiction out rather than handing back, and you judged that good — so nobody has yet watched it hand back on a *person's* answer, because it did not need to.

## Done

- **The process is written down** — `process.md` is the single definition every skill obeys. When a skill and it disagree, it wins and the skill gets corrected.
- **The plumbing** — a list of managed projects, a command to copy them onto your machine, and the rule that work happens here rather than inside a client's repository.
- **Ten stage skills exist and have each been exercised against throwaway projects:** onboarding a new repository, sorting an incoming request, interviewing you for requirements, writing requirements down, recording architecture decisions, planning work into steps, building the code, checking the result, opening the pull request, and — new on 2026-08-02 — **acting on your feedback**.
- **A standards library** — twelve written entries. Ten cover the preferred technology choices; two apply to every project with no exceptions (accessibility, which is a legal requirement in the EU, and visual/interaction design). The twelfth is new today, below.

## Just finished — you can answer by just writing back

**Built 2026-08-09, the same day the rule was written.** The entry below changed the rule: any ticket waiting on you offers two ways to answer, write it there or talk it through. Until this afternoon only the *words* existed. The machine wrote them onto tickets and then acted on neither path — so "write your answer here" was a sentence, not a thing that happened. Now it is a thing that happens.

**Three pieces, and they only matter together.**

- **Any ticket waiting on you now offers both ways, and picks neither for you.** Write the answer in the thread, or run the one line the ticket hands you. Same session either way, same record written back onto the ticket.
- **The questions on a map are watched like any other ticket.** The machine now recognises a map question as something being asked *of you*, rather than a fresh request somebody just filed — so it stops trying to sort it, and instead parks it waiting on your answer. That is also what makes the run-this-line instruction work at all: until today the command **refused** those tickets outright, because that half of the machine had never been told they exist.
- **Writing the answer moves the ticket.** A comment of yours on a waiting ticket gets picked up within a minute and the session carries on from there — no keyword, nothing to remember, the same way your approvals already work. It reads everything you wrote after the question, not just your last comment, so a second thought typed a minute later is not thrown away.

**The limit is built in rather than promised.** If what you write leaves something open, you get asked once more — **only the part still open**, never the whole question again. If that answer still doesn't settle it, the machine stops typing at you and hands the conversation back. That bound is the entire reason writing was allowed back in, and it is now something the machine does rather than something the rulebook says.

**It has now been watched doing all of it, on the throwaway app rather than on yours.** Two questions were put on the to-do app on 2026-08-09 and answered in writing, with nobody touching a command:

- **Answered fully**, the machine picked the comment up on its own within a minute, worked out the answer, wrote it back, closed the question and added the one-line summary to the map — two and a half minutes, start to finish. It also went and looked at the app rather than asking again, and told you which part of your answer was already how the app behaves.
- **Answered half**, it came back **once**, asking only about the half left open and recommending an answer. Told something genuinely contradictory the second time, it stopped: it said the two things wanted pull opposite ways, kept the half already settled, and handed the conversation back with the one line to run. **No third question.** That was the one test worth running, because a machine that simply read comments would have passed everything else and still been able to badger you forever.

**Checked independently on 2026-08-11, and it did not pass** — see the section above for what was found. The description below is what the machinery is *meant* to do and mostly does; the two things wrong with it are at the top of this file. 662 automatic checks pass, and none of them sees either fault. And the answers above were typed by the machine playing you, on throwaway questions about the to-do app, **because you said not to use the trading app as a guinea pig** — so the machinery is proven, and ~~the only thing still untested is whether a real person would agree it gave up at the right moment~~ **you closed that on 2026-08-14 by answering three throwaway questions yourself** (top of this file).

~~**Three of the nine questions on the trading app are now connected to it; six are not, and that is deliberate.**~~ **Superseded on 2026-08-09/11.** All three were answered and closed, along with two more, and the map moved on: six further questions are now open and connected, and they got connected as their blockers closed exactly as intended — a resolving session went back and rewrote the tickets that were waiting on it. **That refreshing is not guaranteed by anything**, though; it was a session choosing well rather than a rule, and it is recorded as a known gap below.

## Before that — a ticket you can just answer

**This one started with you noticing something was wrong, and you were right.** The trading app (`ivtrends`, a separate repository) has a map of open questions on GitHub — **nine of them needing you**, not the six first reported here; that was a miscount, corrected the same day. Every one was a clear, well-argued question. **Not one of them said what to do about it.** No reply instructions, no command, nothing. And the command you *would* have needed refused to run on them, because that side of the machine had never been told these questions exist.

**What changed, as a rule.** Any ticket that is waiting on you now offers **two ways to answer, and you pick without explaining why**:

- **Write the answer there.** A comment is enough. You don't have to answer every part, and "I don't know, what do you suggest?" is a real answer.
- **Or talk it through**, by running one line the ticket hands you — for when it's easier said than written.

**Both end up in the same place.** The same session picks it up either way, and the same record gets written.

**The catch that had to be designed around.** Answering in comments was deliberately ruled out back on 2026-08-02, and for a good reason: an interview is fifteen questions, and fifteen questions in a comment thread is miserable. That still holds. What was missed is that these map questions are **one sharp question each** — which is a different thing, and typing six words into a terminal to answer one of them is silly. So the written path comes with a limit: **if what you write leaves something open, you get asked once more, and if that still doesn't settle it the machine stops typing at you and hands back the conversation.** Without that limit it would have quietly become the thing that was ruled out.

**What is done and what is not.** The rules are written down and committed — the process definition, the skill that makes those maps, the requirements list and the decision record. **The machine does not do it yet.** That is `doc/plans/phases/phase-18.md`, **which you approved on 2026-08-09** and handed to another session to build.

**The nine live questions are no longer bare — I wrote their instructions by hand.** Every one now says what to do about it. They carry the **write-it-here** path only, and say in as many words that the talk-it-through option isn't available yet, because **an instruction you can't follow is the same bug in a nicer suit** and shipping one to replace the other would be no fix at all. When the build lands, those hand-written blocks get *replaced* by the real ones — not added to, or a ticket would end up with two sets of instructions disagreeing about what exists.

**They don't all say the same thing, because they aren't all in the same state.** Three are ready to answer now. Five are waiting on one of those three, and say so by name rather than making you work it out. The ninth isn't a question at all — it's a screen you'll be shown and asked to react to, once the ones it depends on are settled.

**And a stale line got fixed on the way.** Three of the ready ones still said they were blocked by research that finished days ago. Left alone, each would have sat directly above a new line saying nothing was blocking it. They now say what that research concluded and how it narrows the answer — struck through, not deleted.

## Before that — you can leave it running overnight

**Phase 17 is complete, built and proven on 2026-08-08.** This is the fault you called *"more a bug I can live with"* when you split it off from previews — and it is now dead.

**What was wrong.** The service proved a job alive by hearing from it every thirty seconds, and killed jobs that went quiet for two minutes. Sound enough, except a sleeping laptop silences the job and the service *at the same time* — so a perfectly healthy job going quiet looked exactly like a dead one. Your Mac's own sleep log showed 113 stretches of silence past that limit in a single night. Left running, it would have killed healthy work and thrown it away, over and over.

**What changed.** The service now writes down when it was last awake and watching. It may only call a job dead if it can vouch for having been present for the whole stretch it is judging. **Silence nobody was listening for stops counting as evidence.** Nothing else about the mechanism moved — no new clock, no asking the operating system anything.

**How it was proven, in twelve minutes and for nothing.** A healthy job was frozen until it looked dead by every measure the old rule used — 161 seconds quiet against a 120-second limit — and the service left it alone and printed why. Then, awake throughout, a genuinely dead job was killed on schedule and restarted with one command. **That second half was run first and on purpose:** a "fix" that simply stopped killing anything would have passed the first test and been a disaster.

**Two faults the proof run found, both in what the service *says* rather than what it does.** It reported "nothing was watching for 0s" — nonsense, and it had three different reasons collapsed into one sentence. And its countdown printed "1m" twice in a row so it looked stuck. Both fixed. **No test could have caught either**, because every test checked behaviour and these were failures of explanation. That is now three phases running where the thing the tests could not see was the thing you would have read.

**Also fixed: the two clocks now say which is which.** The live line says `4h13m elapsed` (wall time, including while you slept) and the closing line says `19m30s working` (time actually awake). Both numbers were always right; they were sharing a name, which made them look like a contradiction.

**Still broken, and now known to be a separate fault:** the live count of how much a job has written. It froze at 4.7k for four hours on a job that used no helpers at all, so the "it's the helpers" explanation does not cover it. It was assumed to share a cause with the clock; it does not, and it now stands on its own.

## Before that — a pull request you can click

**Phase 16 is complete and you signed off its live gate on 2026-08-08.** Until today a pull request from the machine gave you words: what changed, the checks it passed, two independent reviews. Now it also gives you **a link**. Click it and you get the app itself, running the code from that branch, on your own machine. It rebuilds within a minute of every push to the branch, and it disappears when the pull request does — containers, database and working copy all removed, not left to pile up.

This was the piece that kept getting pushed back. It was next in line three separate times and lost its place each time; this phase was previews and nothing else.

**What you can and cannot do with it, said plainly rather than left for you to discover.** It runs on this laptop, so it works when you are at this machine and nowhere else — **not from your phone**, which was the thing that made the question worth deciding in the first place. And the data in it is whatever sample data the project itself commits — never a copy of anything real, and that is a refusal rather than an omission, so "let's make the preview realistic" can never become the reason a copy of real data appears.

**One thing you will notice: the address changes.** Each rebuild gets a new one, because the machine asks Docker for a free port rather than handing them out itself — which is what stops two previews colliding and means nobody has to keep a list. The comment on the pull request is **rewritten in place** each time rather than a new one being added, so there is only ever one of them and it is always current. You were told this before approving and again at the sign-off, and did not object; if it grates in practice, say so.

**The proof run found three faults that 581 automated tests could not**, which is the pattern by now:

- **A broken build told you "Dockerfile:74".** True, short, and useless. Docker prints its actual explanation last and its noise first, and the machine was reading the first line. It now reads the useful one — the same broken build now says the build command failed and which one.
- **Timone's own tests started failing on the to-do app's tests.** A preview puts a copy of a client's code inside Timone's own folder, and the rule that keeps client code out of Timone's test runs knew about one such place, not two. Every future test run would have failed on somebody else's tests.
- **A failure message was about to publish this laptop's folder paths onto a client's public pull request.** Caught before any of it ran for real, by pointing the machinery at real Docker early instead of trusting the stand-in.

**A fourth turned up after you had already signed off**, while I was tidying up — and it is the one I would have missed by stopping when you said yes. **Cleaning up a finished preview was leaving 1.5 GB behind on your disk, every time.** The containers went, the database went, the working copy went; the *built application image* stayed, one per pull request, forever. Worse, **the standard way to fix it quietly does nothing** — the command reports no error, removes nothing, and looks exactly like success. It is now done a different way, and I checked your disk after a real clean-up rather than trusting the command.

**Two things it does not yet prove, written down rather than glossed.** The test pull request was opened by hand rather than produced by the machine's own process end to end — that was your call, to avoid re-running the whole pipeline at roughly $27 to reach a thing you could reach in a minute. And "your review comment gets acted on *and* the preview updates" has still never been watched happening together. **Both close for free on the next real ticket that gets that far.**

## Before that — the machine no longer blames the wrong session

**Phase 15 is complete and you signed off its live gate on 2026-08-08.** You chose on the same day to fix the faults phase 14 found **before** the click-a-preview work, so previews move back one more place, to phase 16. Two of the three are now fixed and the third is understood.

**The one that mattered: it could publicly accuse the wrong session, and it can't any more.** When you worked in the Timone folder while the service was building something, the safety check could attribute your commits to its job — and on 2026-08-07 it posted a public comment on the to-do app's ticket, under your GitHub account, accusing the machine's own build of touching three files it had never gone near. Every commit already carries a label saying which session made it; the rule simply wasn't reading it. Now it does.

**How that was proved, because "it went quiet" is not the same as "it was fixed".** The exact situation was recreated — the service building while another session worked alongside it — and the same commit was shown to two sessions at once: **the one that made it named it; the one that didn't stayed silent.** Then a *real* mistake was made deliberately, to confirm the alarms still go off. They did, and the public comment they posted named **exactly one file** — the one actually touched — where the old comment had named three. **The risk was fixing it by quietly switching the alarms off, and that is ruled out.**

**The false accusation was corrected in public**, on your call: a reply under the original saying what was wrong, why, and that it is fixed — rather than deleting it, so the record of the mistake stays legible.

**Also fixed:** restarting a failed job no longer carries over the previous attempt's warnings, so it stops complaining about files that no longer exist.

~~**And the sleeping-laptop clock is now explained** — see "Still to fix" below. It is not yet fixed, and the overnight warning still stands.~~ **Explained on 2026-08-08 and fixed the same day** — see the top of this file.

---

## Before that — you can watch it work, and it says what it costs

**Phase 14 is complete and you signed off its live gate on 2026-08-08.** Four things changed. Each step now runs on a machine suited to it, so sorting a ticket no longer costs what building a feature costs. While a session works, it tells you every thirty seconds how long it has been going and how much it has written, and one line at the end saying what it actually cost. That same heartbeat is what proves it alive, so a service killed halfway through no longer leaves a job wedged forever with your project locked behind it. And the automatic safety checks moved to a place **every** session passes through — including the ones you run yourself in a terminal.

**The first real price tag.** One ticket on the to-do app — "I can't run this anywhere but my own laptop" — went from plan approval to a merged pull request for **$27.06**, and the two steps that spread work across helpers accounted for most of it. That is the first end-to-end cost this project has ever measured.

**The gate went further than a clean pass would have, and this is the honest part.** Running it for real found **six** faults that 532 automated tests never could. Three are fixed: the machine was naming the step a job had just *left* rather than the one it was in; a log file at the root was accusing every job that ran; and **every instruction it wrote you named a command that did not exist on your machine** — you found that one by trying to do what a ticket told you to do. Three are recorded and not yet fixed:

- **It can blame the wrong session.** When you work in the Timone folder while the service is building something, the safety check can attribute your commits to its job — or its commits to your session. It did both. Once it posted a public comment on the to-do app's ticket accusing the machine's own build of touching three files it had never touched. The information needed to get it right was sitting in those commits the whole time; the rule simply does not read it.
- **The running count of how much it has written is wrong**, by between two and six times, and always too low. The closing cost line is correct — it is the live one that misleads.
- **The clock in that live line is wrong when your laptop sleeps.** On an overnight run it claimed 4 hours 13 minutes for 19 minutes of actual work.

**That last one matters more than it looks, and it is why one promise is being held back.** The same heartbeat that shows you progress is what proves a job alive — so a sleeping laptop makes a perfectly healthy job look dead. On that overnight run it went quiet **17 times** for longer than the service's patience allows, once for a full 16 minutes. Nothing was lost, but only because the gate was being driven one step at a time by hand; left running normally, **it would have killed a healthy job and thrown its work away, seventeen times over.** ~~Until that is fixed, do not leave the service running unattended overnight on a laptop that sleeps.~~ **Fixed and proven on 2026-08-08** — see the top of this file.

**What that means for the sign-off.** Two of the phase's five promises are signed off (a machine per step; every commit saying which step made it). Three are deliberately **not**, and each has the reason written next to it. Nothing was quietly marked done.

## Just finished

**It now asks you things, and waits properly for the answer** (built 2026-08-03, **signed off by you 2026-08-05**). The machine no longer stops dead after sorting a ticket. It carries on to the point where it needs you, and there are exactly two ways it can need you:

- **A single decision** — it posts what it wrote, links the actual document on its own branch, and tells you the one word to reply. Reply `approve` and it carries on. **Reply anything else at all and it treats that as a change**, and does the step again with your words in hand. It never guesses: "approve once you've fixed the wording" is a change, on purpose, because a gate that reads intent eventually approves something you never approved. And it cannot approve itself — its own comments are marked, so quoting the word back at the ticket does nothing.
- **A conversation** — when the step needs a back-and-forth rather than an answer, the ticket hands you one line to paste into your terminal (`timone takeover scratch-app#6`). Running it works out what that ticket is waiting for and starts the conversation there. You never name a stage or a tool. What the two of you agree gets written back onto the ticket; the conversation itself is not kept.

After that conversation it writes down what you're asking for, commits it, and asks you to approve it; then it plans the work, commits that, and asks again. Approving is what unblocks building — which is the *next* thing to build.

**What was watched working, on the to-do app, with you in the loop:** ticket #6 started by itself, was sorted, and came back with a written invitation to talk it through in your terminal. You ran the command, had the conversation, and accepted the summary — which landed back on the ticket. It then wrote down what you'd asked for, put it on a branch of its own, and asked you to approve it. You replied with a criticism instead, and it **redid that step using your words**, taking the edit button back out rather than arguing. You approved the second version; it recorded your approval into the requirements themselves and moved on to planning, which produced a five-part plan and asked again. You approved that too, and it stamped your name and the date onto the plan.

**It also cannot approve its own work**, which was tested rather than assumed: a message written by the machine, containing nothing but the word "approve", was posted onto the open question deliberately. It moved nothing.

**The freeze you were told about is gone.** A ticket waiting on you no longer blocks the project. Only a ticket that has reached the point of owning a branch of work does — because from there on, two of them would be two sets of changes to the same code. Still exactly one session running at a time, always. This was your call at approval, and the alternative (simpler, but one unanswered question freezes everything) was rejected.

**The machine picks work up from tickets by itself** (2026-08-03). This is the first piece of the automatic loop actually running rather than designed. You file an issue on a project in whatever words come naturally, put one label on it, and a watcher on your machine notices it within a minute, replies to say it has been picked up, works out what kind of request it is, writes that back on the ticket with its reasoning, and then stops and says so — because the stage that would act on it is the next thing to build.

It was proven on the to-do app rather than described. A ticket reading *"when I add a lot of items the page starts to feel really slow, especially when I tick things off"* came back classified as a bug, with an argument for why that call was close (nobody ever wrote down a speed requirement — but the requirements do promise that ticking something off shows **immediately**, and that is the promise being missed), a likely cause flagged as a lead rather than a verdict, and the observation that this is the same problem you already agreed to fix on 2 August, so whoever picks it up should merge the two rather than do the work twice. Not one stage number or tool name appears in it: <https://github.com/fvermaut/scratch-app/issues/4>

A second issue filed at the same time **without** the label was left completely untouched — the label is a permission boundary, not an instruction. A third one filed while the first was still running was queued, and said so. `timone status` answers, in one line per project, which ticket is in play, how far it got, and whether anything is waiting on you.

**After every automatic session, three dumb reliable checks run** — work committed but never sent, the status file written where nobody will see it, files touched outside the project being worked on. A violation is one loud comment on the ticket; a clean session says nothing at all.

**You caught a real defect at the sign-off, and it was the good kind.** Every message the machine posted appeared under your name, because it posts using your GitHub login — so the ticket read as though you had written your own acknowledgements and your own verdicts. Beyond being confusing, it was quietly dangerous: approvals are meant to happen as replies on the ticket, so the machine would eventually have been able to read *its own comment* as your approval. Every machine message now carries a line saying so, the machine can tell its own words from yours when it reads a thread back, and the messages posted before the fix were gone back over and marked. A proper robot account is the real answer and is written down as its own piece of work — it needs a credential from you.

**Four things this does not yet prove, written down rather than left implied.** The rule that *you* never have to name a stage when you type here directly is in force but has never actually been watched working — the next request you type settles it. The check that a session stays inside the project it was given passed on a session that wrote no files at all, so it held for want of anything to catch. Two of the three automatic checks have only ever fired in tests. And a queued ticket has never been seen starting, because nothing has yet been able to finish.

~~**One consequence you should know:** a ticket waiting on you counts as the project being busy.~~ **Settled on 2026-08-03** — see the freeze paragraph above. You chose the rule where only work that owns a branch blocks a project.

**The automatic loop was designed, and its first phase written down** (2026-08-02, evening). You sat through a nine-question interview about how the machine should talk to you, and every question got settled. The decisions, in plain terms: **approvals** (yes/no moments — approve these requirements, approve this plan) will happen as replies on the ticket, and only there; **interviews** (like the one you just did) will *not* happen as ticket comment ping-pong — they get their own channel, eventually Slack, but first a simpler fallback: the ticket tells you exactly one command to run, and that command opens the interview in a terminal on your machine. You never need to know what stage anything is in or what any skill is called — **you write requests in plain language and the machine works out what to do with them**, a rule that now applies even when you type here directly. Every message the machine sends you must end with a clear "here's what I'm asking you to do" line. And after every automatic work session, a dumb, reliable checker verifies the three rules agents have been caught breaking silently (work committed but never sent, the status file written in the wrong place, files touched outside the project being worked on). Two decision records were written (`doc/adr/0012` and `0013`), the requirements for the automatic loop were amended accordingly, and the first build phase — the part that watches tickets and sorts requests — was planned in `doc/plans/phases/phase-11.md`. **You approved it on 2 August, and it is the entry above.**

**The loop closed for the first time** (2026-08-02). This is the result phase 10 existed to produce, and it is worth stating as one sentence: a finding from a pull request review became a change to the to-do app's written requirements, then a plan you approved, then code, then an independent check, then **a new pull request now waiting for you** — <https://github.com/fvermaut/scratch-app/pull/3> — and the feedback stage itself wrote **not one line of code** along the way. That last part is the design working, not an accident: everything the feedback stage decides to change goes back through plan → build → check → deliver, so nothing reaches you unchecked and unreviewed. Until today the process could take work from an idea to a merged pull request but had no way back in; now it goes round.

**The feedback stage is built, and it has processed the first real feedback this workspace has ever held** (2026-08-02). This was the last missing skill. It does one thing: it works out what a reaction to finished work *means*, proposes a response, and — once you have confirmed each item — hands the work to the stages that already exist. **It never changes code itself.** Everything it commits is a document; every code change it decides on goes back through plan → build → check → deliver, so nothing lands unchecked. Four batches of real feedback went through it on the to-do app and are recorded there permanently: the day-one "completed todos reappear" bug (closed as *already fixed* — it was filed six days before the app existed, and the skill recognised that rather than inventing work), the nine review findings from the first pull request (you confirmed seven, declined one, postponed one), and two cases where a written record had gone stale and was corrected. One of your decisions changed the product's requirements — see below.

**Building it took seven rounds of corrections, and the pattern from earlier phases held: the defects that mattered were invisible to reading.** Three of the first four trial runs hit the same wall — the skill could diagnose "a document is wrong" but had no legal way to fix the documents that were actually wrong, so a whole class of review findings had nowhere to go. The two worst were only findable by *doing* it. First: the skill wrote records to your machine and never sent them anywhere, so a "permanent record" existed on one laptop. Second: when you change a requirement, the old evidence for it stops counting — and the skill was quietly switching off that safety net for other work being fixed at the same time. Neither is visible on the page; both are obvious the moment the thing runs.

**It also forced corrections to four other skills**, all for the same reason: the feedback stage sends work into stages that were written before it existed. The planning stage would have *refused* the commonest thing feedback produces. The checking stage never mentioned that starting the app is a command that never finishes — two checking runs simply hung.

**One of your decisions changed what the to-do app promises.** A review found that the built app can serve a stale list on first load. You judged — correctly, and against my earlier reading — that no requirement ever said otherwise, so this was not a bug but a gap in what we had asked for. The requirement was amended first, in the proper way (same ID, marked *revised*, dated), and the code work queued behind it. That is the whole point of the feedback stage: it separates *we asked for the wrong thing* from *it was built wrong*, and treats them differently.

**Prototypes are in — react instead of read** (2026-08-01, decided in a five-question interview with you). A prototype is a rough, clickable version of an idea, built cheap and thrown away, existing so you can *react* to something instead of reading a document. It now serves two moments: settling a "how should this look or behave?" question on a discovery map (the entry below), and the requirements stage — where, for anything with a user interface, you can be walked through a clickable mock while approving the requirement list, instead of only reading it. Two rules keep it honest. First, **approving a mock never replaces approving the written requirements** — a mock always shows less than the requirements say (what happens on errors, what is mandatory versus nice-to-have, accessibility), so the agent must point out every place the mock and the list differ before asking for your sign-off. Second, **prototype code is built to die**: it lives on its own branch, is never merged, never becomes the starting point of real work, and is deleted once your reaction is recorded — the reaction is what is kept, not the code. A small dedicated skill, `timone-prototype`, owns that whole lifecycle. Decision record: `doc/adr/0011-prototype-convention.md`. Untested caveat: the machinery meant to serve these mocks as clickable pages (the Docker preview decision from day one) has never actually done so — the first prototype is its test.

**A new skill for big, foggy ideas** (2026-08-01). Until now, digging requirements out of you happened in a single interview sitting — fine for one feature, hopeless for an idea too large to settle in one conversation. The new `timone-wayfind` skill charts such an idea as a **map**: a list of open questions kept on the project's issue tracker, each sized to one working session, with the dependencies between them drawn in so it is always visible which questions are ready to tackle. Sessions then resolve them one at a time — interviewing you, or researching alone — until nothing is left to decide and the requirements get written down the normal way. The map is scratch paper, not the record: any decision that matters is promptly moved into the permanent documents, because tickets organise work and never hold the truth — a rule you set on day one, which this design keeps (the reasoning is recorded in `doc/adr/0010-wayfinder-discovery-maps.md`). It is adapted from a published skill by Matt Pocock, with one deliberate cut: it never builds anything — building has its own stages. (A second cut made that morning, leaving out its "prototype" question type, was reversed the same day — see the entry above.) It has not yet been used on a real idea — the first oversized one is its test.

**The delivery skill is built, and it has opened two real pull requests** (2026-07-28). This was the last stage before feedback.

Delivery does one thing: it puts finished work in front of you, with everything you need to judge it. It does not improve the work, does not merge it, and does not decide anything the pull request exists to let you decide.

Two independent reviews run before the pull request opens, and **they are deliberately never blended into one list**. One asks *"does this follow the conventions we agreed?"*. The other asks *"did it build the thing we actually asked for?"*. They are kept apart because code can follow every convention while building the wrong thing, and can build exactly the right thing while breaking every convention — a single merged list hides that difference.

**Neither review is allowed to change anything.** They report; you decide. Anything worth acting on goes through the feedback stage, which is the next skill to build. This resolved a genuine contradiction in `process.md`, the written rulebook every skill obeys: the building stage had been telling its agents "leave the tidying-up to the delivery review", which reads as a promise that delivery *does* the tidying — while the delivery stage said the reviews only report. Delivery cannot be allowed to change code, because that code would land *after* the checking stage signed off and *before* you read it, quietly invalidating the sign-off at the moment it is being shown to you. So the building stage's sentence was the one that was wrong, and it has been corrected.

**A twelfth standards entry was written and you approved it:** a fixed list of code smells (`standards/code-smells.md`) — twenty things a reviewer looks for in a change, each with the signal that identifies it. The conventions review had been specified against this list for weeks, and the list had never existed.

**Two pull requests were opened on the to-do app. You merged the first on 2026-07-29** — the first work Timone has ever put through the whole process end to end, from an empty repository to merged. The second is still open and listed under *Waiting on you*.

## What the proof runs found

**2026-08-05 — the asking-and-waiting machinery asked you to approve a plan it had never written.** The worst possible failure for a thing whose entire job is asking permission, and the most useful one to have found on a throwaway app rather than a client's. Two separate faults sat behind it.

**The first was simple.** The machine decided it had finished the planning step because the session ended without an error — never because a plan actually existed. It then posted a perfectly-worded approval request pointing at an empty folder. Had you replied `approve`, it would have moved on past a step nobody had done. It now checks that something was actually written before asking, and says so plainly when nothing was: *"there is nothing for you to approve, and I would rather say that than ask you to sign off on a blank."*

**The second was the interesting one.** Two of the machine's own instruction files said the human approves the plan *before anything is written down*; the newer requirements for the automatic loop said the opposite — write it on a branch, then ask. Both had been signed off, months apart, and nobody noticed they contradicted each other. Then two sessions, minutes apart, read the same contradiction and went opposite ways: the first wrote the requirements down, the second refused to write the plan. **Neither was disobeying. That is what makes it dangerous** — an instruction that contradicts itself doesn't fail loudly, it just makes the machine unpredictable.

Rather than patch it, we sat down and settled it: **write the thing, then ask about it.** You read the real document instead of a summary of a document. That decision is now written down as a permanent record, and the four places that disagreed were corrected to match. It also forced a question nobody had asked — when do requirement numbers become permanent? They now freeze when you approve, not when they're first typed, so a draft you reject no longer leaves numbered ghosts behind forever.

**Previously:** four runs against the to-do app, and three rounds of corrections. **Two of the three corrections were rules that contradicted themselves** — the most useful kind of finding, because nothing fails visibly when an instruction is impossible to obey; the agent just quietly picks one half.

1. **The code-smell list was too narrow.** It described duplication as "the same block appearing three times", so a single repeated *line* did not count — and a one-line cache instruction copied into all three places that needed it went unreported. The list now says a repeated one-liner counts. Re-run against the corrected list, a fresh reviewer put exactly that finding first.
2. **The reviewers were told to read every file in the change, and also forbidden to read one of them.** A finished piece of work always contains its own checking report, so the two rules could never both be obeyed. The reviewers are now told plainly that the paperwork is not what they review.
3. **Re-delivering re-reviewed its own paperwork, forever.** The rule said "re-run the reviews when new work arrives" — but the delivery report *is* new work on the same branch, so every re-delivery would have re-reviewed what the last one wrote, and the next would re-review that. Re-running is now tied to the code actually changing.

**One result is worth your attention, and it is not a defect.** On the to-do app, the checking agent ran the real application and confirmed that deleting an item removes it immediately. The conventions-and-requirements reviewer read the source code instead, and found the code openly admits that its normal path sometimes fails — so it schedules two backup refreshes, a quarter-second and a full second later, with a comment conceding the first one can lose the race. **Both are true of what each looked at**: one watched it work, the other read why it might not. That disagreement is printed in the pull request rather than resolved, because resolving it would have meant hiding one of them.

## Previously

**The checking skill was finished and signed off** (2026-07-28). An agent that never saw the to-do app being built now checks it against its promises — running the real application, never reading its code or its tests. What it cannot check by machine, it writes up as a script for you rather than guessing. What fails, it hands to a separate agent to fix, then re-checks everything from scratch. Report: `doc/plans/phases/reports/phase-08-complete.md` (Timone repository).

That took three rounds of corrections, and the middle one is the one worth remembering. **The checker's first attempt at a deliberately planted bug returned "everything passes" — and it was wrong.** The requirement says a title typed with spaces around it gets stored without them. To test that, the checker sent a padded title to the app — but the tool it used to send it strips padding itself, before the app ever sees it. So the test performed the very trimming it was supposed to be checking, and could only ever agree. Worse: the app's own tests were failing on exactly that behaviour at the time, and the checker wrote that contradiction down as a curiosity for you rather than treating it as the alarm it was. Two rules now close this: **a check must be shown capable of failing before its "pass" counts**, and **when the app's own tests and the checker disagree, the checker stops and works out which instrument is lying**.

**The code-writing skill was finished and signed off** (2026-07-26). Proving it meant pointing it at a throwaway to-do list app and telling it to build the thing from scratch. It did. That took seven rounds of corrections — roughly 35 defects — because every real run exposed something the instructions got wrong. Report: `doc/plans/phases/reports/phase-07-complete.md` (Timone repository).

The seventh round is worth singling out: **the rule for choosing where to start work meant no project could ever begin its second piece of work.** Git, the version-control system, keeps work on named lines of development called *branches*; the main line is called `main`. The rule said start from `main` — but finished work sits waiting for your approval rather than joining `main` immediately, so `main` had no code on it at all. Found while preparing a test rather than running one.

## What's left

| Stage | What it does | State |
|---|---|---|
| **Feedback** | Takes your "that's not what I meant" and works out whether the requirements were wrong or the code was | **Built, exercised on real feedback, and signed off 2026-08-02.** No longer a gap. |
| **Deployment** | Releases approved work to a live environment | Deliberately later — defined in `process.md`, the written rulebook every skill obeys, no skill yet |
| **Maintenance** | Turns dependency updates and production issues into new incoming requests | Deliberately later — defined in `process.md`, the written rulebook every skill obeys, no skill yet |

**Then** the second half of the vision: instead of you typing instructions, a background service watches your tickets and drives these same stages itself, posting questions and results back to the ticket. ~~What remains, in build order: the part that builds code and opens pull requests~~ — **built and signed off 2026-08-06** (see "Just finished" at the top): a ticket now travels from plain language to a merged pull request with your only inputs being answers, approvals, review comments and the merge. **You can start using it on a real pet project.** What remains, in build order: ~~the click-a-preview machinery (phase 14)~~ **— you moved this back one place on 2026-08-06**, so: ~~first making the machine watchable and its own sessions accountable (phase 14, plan awaiting your approval)~~ **— done, gate signed off 2026-08-08**, with three of its five promises deliberately held back and the reasons written down; ~~then the click-a-preview machinery (phase 15)~~ **— you moved this back a second time on 2026-08-08, to fix the faults the phase-14 gate found; phase 15 did that**; ~~so the click-a-preview machinery is now phase 16, together with the sleeping-laptop fix~~ **— and later the same day you split those two apart and put previews first**; ~~the click-a-preview machinery is phase 16 (awaiting your approval)~~ **— built and signed off 2026-08-08, and it was not displaced a third time**; ~~so what remains is **the sleeping-laptop fix, phase 17, whose plan you approved on 2026-08-08 and which is the next thing built**~~ **— built and proven the same day**; so what remains is **Slack, then the bug-report path** (your ticket #4 on the to-do app is parked waiting for exactly that).

## Just finished — the machine now goes all the way to a merged pull request

**Phase 13 is complete and you signed it off on 2026-08-06.** The missing back half of the automatic loop is built and was proven on the to-do app with you in the loop, end to end: ticket #6 — "typing in the box is fiddly on my phone" — became five built-and-checked slices of code, then a pull request with everything you needed to judge it (what changed in plain words, the full table of checks it passed, and two independent reviews by fresh eyes), then a merged, **closed** ticket. You never named a stage or a tool once.

The proof run also showed the safety machinery earning its keep, which matters more than a clean pass would have:

- **It refused to build on a stray leftover.** A note from your email-alerts request had been accidentally saved onto this ticket's work by an earlier hand-run session. The builder found it, stopped, explained, and asked — twice, because your first reply never actually reached the ticket, and it would not take anyone's word for it but the ticket's. After your `move it and build`, it filed the note where it belonged and built.
- **Your review comments drove real work.** Your concrete comment (derive the placeholder row's height instead of hard-coding it) became one focused fix, a full re-check of everything, and the same pull request updated with a reply. Your deliberately vague one ("not sure about the spacing") got you a clarifying question listing what "spacing" could mean — and no code change, on purpose.
- **Failures had a road back.** Three times a run stopped and `timone retry` restarted it exactly where it stood — the hand-editing of internal files that plagued the last sign-off is gone.
- **The merge finished the job.** Your merge closed the ticket, and the test ticket queued behind it started within the same minute — the first time the queue has ever been watched promoting for real.
- **Two defects were found live and fixed on the spot:** a session that ended while "waiting to be notified" of background work (its work died with it), and finished tickets being left open — the second one caught by you at the gate.

## Waiting on you

**What I need from you now: three things. One is urgent, one is a ruling only you can make, and one is a single word.**

- **Settle one thing about lists of questions, because it decides what happens next time.** When a list of questions finishes and you say go ahead, it writes the specification — and then, as it stands, **the same ticket carries straight on into planning and building the whole feature.** Nobody decided that; it just follows from how the handover was built. I stopped it on the throwaway app rather than let it build something nobody wanted. **Should a finished list of questions become the thing that gets built, or should it stop at the specification and hand over to a fresh ticket?** Say either and it gets recorded properly.
- **One contradiction inside the rules you approved on 13 August, and it is yours to resolve.** You asked for two things that cannot both be true of the same ticket: *every open ticket must say what happens next*, and *a repository joined with a big backlog stays completely silent*. So an unlabelled ticket on a silent repository says **nothing at all**. Three separate build steps noticed this and each deliberately left it alone rather than quietly pick a side. **Either the silent-backlog rule excuses those tickets from the first rule, or they are owed a line even when they get no introduction.** Nothing is blocked on this, but the promise cannot be signed off until you say which.
- **Restart the watcher before you write your next answer on a ticket.** Stop `timone daemon` and start it again. A running watcher keeps running the code it started with, so until you do, the duplicate-answer fix is not live on your machine — and this was watched happening during the proof run, not guessed at. **After that, the trading-app questions can be answered in writing again**, or talked through, whichever suits you.
- **One decision, whenever you want to make it, and nothing is blocked on it.** The rulebook says that when your second answer still doesn't settle something, the machine stops and hands the conversation to your terminal. On 14 August it **worked the contradiction out instead**, and you called that a pass. So the written rule and the actual behaviour now differ. **Should the rule change to match what it did, or should the behaviour change to match the rule?** Say the word either way and it gets recorded properly; leave it and it sits here as a known disagreement.

~~**What I need from you now: approve `doc/plans/phases/phase-18.md`**~~ — **approved 2026-08-09, and built the same day.** See the top of this file for what it does and what it has not yet proven.

~~**What I need from you now: answer three questions on the trading app.**~~ **You answered all three, and two more besides** — `ivtrends` #5, #6, #7, #8 and #9 are settled and closed, each by talking it through in your terminal, each leaving a written record on its ticket. Two of them produced permanent decision records in the trading app's own repository (its ADR-0006 and ADR-0007).

~~**What I need from you now: two rulings on Timone itself, before you answer anything else in writing.**~~ ~~**You made the first on 2026-08-11. The second is still open.**~~ **Both are ruled, and as of 2026-08-14 only one of them is still waiting on a build — problem two's.**

- ~~**Problem one above — one answer sets two of the machine going.**~~ **Ruled on 2026-08-11, built and proven on 2026-08-14.** See the top of this file. You were shown four options and chose all three fixes layered rather than the cheap one. The reasoning is written down as a permanent record (`doc/adr/0023-one-answer-one-session.md`) and the work is planned as `doc/plans/phases/phase-19.md`, **which is what I need you to approve.** What the trace found, in one line: the machine has nothing stopping two watchers running at once, it puts its name on a job only *after* starting the work, and the check that should catch that reads a stale copy. There is also a fourth fault needing no second watcher at all — a session that answers you without saying anything reads your comment again next time round.
- ~~**Problem two above — the requirement says one thing, the build does another.** Reword the requirement, or build the missing path?~~ **Ruled on 2026-08-13, as a side effect of the bigger question above.** You chose **build the missing path**: the takeover command will resolve any open ticket by reading it, rather than refusing anything it has no record of. The requirement stands unreworded, and it is built as part of phase 20. The refusal sentence you hit in August is retired.

~~**What I need from you now: approve `doc/plans/phases/phase-19.md`.**~~ **Approved 2026-08-11**, and handed to another session to build. **You corrected one thing before approving, and it stuck:** the plan's proof run had you answering a real trading-app question in writing, on the reasoning that six of them already existed and were therefore free. You said use the throwaway app, that is what it is for. It now runs entirely on the throwaway app, and the rejected reasoning is written down rather than quietly swapped out — because *"it's free over there"* is the argument that will come back.

✏ **2026-08-13: phase 20 is approved**, and you said you would run the build in another session. ~~**Phase 19 is still unbuilt and still comes first**~~ **Phase 19 is built and proven as of 2026-08-14**, so phase 20's precondition is met ~~and it may go ahead~~ — **and phase 20 was built and proven the same day.** See the top of this file for what it does and for the two promises it deliberately does not claim.

~~**What I need from you now: nothing until the build asks for you.** The next session builds phase 19 and comes back with something to look at. **It will need you for one thing at the end** — answering a throwaway question on the to-do app in your own words, and saying whether the machine's reply was a fair reading of it.~~ **Done on 2026-08-14** — you answered three, judged all three, and that is what lifted the write-your-answer restriction. It was the one judgement no test could make, and it is the only reason that promise counts as proven.

~~**Six new questions are open on the trading app**~~ **Five, as of this evening** — you answered and closed #10 at 19:17 by talking it through, after this file last said six. Open now: `ivtrends` #12, #13, #14, #15, and #11, which is a screen to look at rather than a question. They are connected to the machine and they are waiting on you. ~~**Until problem one is fixed, talk them through rather than writing your answer on the ticket** — run the line each ticket gives you. Writing the answer works, but it will answer you twice.~~ **Lifted 2026-08-14: answer them whichever way you like** — write it on the ticket, or run the line the ticket gives you. **Restart the watcher first**, or the machine answering you is the old one.

~~**One of them is telling you something wrong right now.** `timone status` asks you to answer #11; #11's own text says nothing is needed yet, because it is waiting on #10.~~ ✏ **Half-corrected on 2026-08-14.** The terminal and the ticket now print the same sentence, so they can no longer contradict each other — watched on the throwaway app, word for word, and **not on the trading app**. What is unchanged is that neither of them knows a ticket can be waiting on another ticket, so they can be wrong together. Still a known fault, still not fixed.

~~**Second thing, and it takes one word from you: shall I connect those nine tickets to the machine?**~~ — **you said yes on 2026-08-09, and those three are now connected.** They are live: answer one and something happens, or run the line the ticket gives you and we talk it through. **The other six are deliberately still disconnected** — each of them is waiting on one of these three, and connecting a question that can't be answered yet would just have me asking you for something you can't give. They come online as these close.

**One thing that behaved worse than expected, so you know before you see it.** Connecting several questions at once makes all but the first say *"you're in the queue, I'm already working on another one"* — which is wrong: nothing is competing, they are all simply waiting on you. I avoided it by connecting them one at a time, so no such comment was posted. It is written down as something to fix rather than worked around forever.

**One decision on Timone itself.** ~~Approve the phase 14 plan.~~ **Approved 2026-08-06**, signed off 2026-08-08. ~~Do the three recorded faults get fixed before the click-a-preview work, or after it?~~ **You chose "before", on 2026-08-08** — phase 15 fixed two of them and explained the third, and previews move back one place to phase 16.

~~**What is now yours to call: one decision about how the sleeping-laptop fault gets fixed.**~~ **You made that call on 2026-08-08**, from five options laid out with their trade-offs. The fix: the service now has to have been *watching* before it is allowed to declare a job dead — so a job that went quiet while your laptop was asleep is no longer mistaken for a dead one, because nobody was listening for it to speak. The trap you avoided was the option that looks most obvious: making both clocks agree would have made the killing *worse*, not better.

**You also settled how previews get addressed** — a question left open since 2026-07-19 — and told me you want to end up on a managed platform like Vercel rather than self-hosting forever. That changed the shape of the answer: previews are now built so that swapping Docker for Vercel later is writing one new piece, not rebuilding the machinery.

**And you split the work in two on 2026-08-08**, taking the click-a-preview machinery first and judging the sleeping-laptop fault *"more a bug I can live with"*. So: **previews are phase 16, the sleeping-laptop fix is phase 17.** ~~The overnight warning below therefore stays in force longer — it is not lifted until phase 17 is built and you have signed off its gate.~~ **Phase 17 was built and proven on 2026-08-08**, the same day you approved it. You did not sign off its proof run — you told me to run it without you — so that one sign-off is outstanding, and the top of this file names the call I made on your behalf.

~~**What I need from you: approve the phase 16 plan.**~~ **Approved and built and signed off, all on 2026-08-08** — see the top of this file.

~~**What I need from you now: approve the phase 17 plan.**~~ **Approved 2026-08-08**, minutes after you signed off previews — `doc/plans/phases/phase-17.md`, the sleeping-laptop fix. ~~**This is the one that lets you leave the machine running overnight**, and until it is built and its own proof run signed off, the warning at the bottom of this file stands.~~ **Built and proven 2026-08-08.** You can leave the machine running overnight.

~~**What I need from you now: nothing until the build asks for you.** The next session builds it, and it will need you at the machine for the proof run — that one cannot be done without you, because the whole test is letting the laptop actually go to sleep with work in progress.~~ **Built, proven and closed on 2026-08-08, and it did not need you after all.** That prediction was wrong in a useful way: the proof turned out to read nothing but the service's own record of when it was last awake, so it needed no real work in flight, no scheduling and no money — it took twelve minutes. You called the original plan overkill and you were right. The screen-reader listen on the to-do app (item 3 below) still stands whenever you have twenty minutes.

**What I need from you now: two things, neither urgent.**

- **Read the one call I made without you** — the overnight warning is lifted, on evidence that froze the program rather than sleeping the laptop. The reasoning is at the bottom of this file. **Say the word and the blanket warning goes back.**
- **Optional, and it costs one lid-close:** leave the service running tonight with something in flight and glance at it in the morning. That turns "proven" into "proven twice" and closes the only gap left in this fix.

**What that approved plan does** — `doc/plans/phases/phase-14.md`. It is the chunk of work you asked for on 2026-08-06, and it does four things: each step of the process now runs on a model suited to it (a quick sorting job no longer gets the same expensive machinery as building a whole feature); while the machine is working it tells you so every thirty seconds — how long, how much it has written, how many helpers it has running — and what it cost when it stops; a machine that gets killed halfway through no longer leaves a job wedged forever with your project locked behind it; and **the automatic safety checks start watching the sessions you run yourself**, not just the ones the machine starts.

That last one is why this went ahead of the previews. On 2026-08-06 a stray commit from a session **you** ran by hand blocked a build, and the check that would have caught it instantly was already written, already tested, already running — it just wasn't looking at your sessions. Alongside it, every commit the machine makes will now say in its own message which step made it, so "where did this come from?" is a five-second answer instead of a reconstruction from memory.

**Cost of the choice, stated plainly:** clickable previews of each pull request move back one place, to phase 15.

**What I need from you:** nothing right now — the next session builds it, and comes back to you at the end with something to look at.

**Previously:** ~~approve the phase 13 plan~~ — done 2026-08-05, after the two rulebook contradictions were settled in a sit-down (`doc/adr/0015`, `doc/adr/0016`). ~~The one-sentence check.~~ **Done 2026-08-05** — you typed a plain request in a fresh session and watched it get sorted first, and then acted on, without naming anything. That closes the last unproven promise from the previous sign-off. One honest caveat, written down rather than glossed: a session you run yourself leaves no ticket, no label and no commit behind, so **that particular check rests on your word and nobody else can go back and re-read it**. Giving those sessions a trace of their own is on the list below.

~~The sign-off for the asking-and-waiting machinery.~~ **Done on 2026-08-05** — you ran all of it: the conversation in your terminal, the deliberate criticism that sent the requirements step back to be redone, the approval that moved it on, the same round trip on the plan, and the attempt to make the machine approve its own work (it couldn't). What that proved, and what it found, is in "What the proof runs found" below.

~~Nothing on Timone itself is waiting on you.~~ ~~Approve the phase 11 plan.~~ **Done 2026-08-02, and built and signed off 2026-08-03** — you approved the ticket-watching slice, it was built, you watched it run on the to-do app, and you passed its sign-off after catching the who-wrote-this defect described above. ~~Re-approve the feedback-stage plan.~~ **Done 2026-08-02** — you re-approved `doc/plans/phases/phase-10.md` after the six-kinds-became-seven change that had cancelled its original stamp. You re-approved it on the evidence rather than on the written argument: by then the seventh kind had been used for real on the to-do app and the loop had closed at a pull request. ~~The last stretch stopped part-way through.~~ **Finished 2026-08-02** — the independent check and the pull request both ran; the check was clean first time with no repairs needed, and the pull request is listed below.

**Previously settled:** You settled the last open question on 2026-07-29: this file is written only on a project's main line, never on a working branch. Every stage rewrites the whole file, so two branches editing it collide the moment the second one merges — a collision in the one document meant to tell you where things stand. That rule is now in the rulebook and in the two skills that write it. **The to-do app still carries the old arrangement on both its open branches; fixing that is listed below.**

**One thing on the to-do app** (`projects/scratch-app`), which has its own status file at `projects/scratch-app/STATUS.md` — now on its main line only. **Both pull requests below have since been merged**, leaving only the listening test:

1. **~~Pull request #1 — the to-do list itself.~~ Merged 2026-07-29. ~~Its nine review findings were deliberately not acted on.~~ Settled 2026-08-02** — you went through all nine at the feedback stage's gate and confirmed seven, declined one, postponed one. The record is at `doc/feedback/002-phase-01-delivery-review-findings.md` in the scratch-app repository, including your reasons in your own words for the two you did not take.

   **The first of those confirmed fixes was pull request #3, now merged:** <https://github.com/fvermaut/scratch-app/pull/3> — binding the app's cache label so a typo cannot silently detach it. Checked independently and cleanly. **It will not merge itself**, and it carries an unticked box: the screen-reader session at item 3 below, put there because the pull request is where you already are when you would otherwise forget it. Its two reviews disagreed in the useful way — the *did-it-build-what-we-asked* review found nothing, and the *does-it-follow-our-conventions* review found two things, neither of which was fixed, because that step reports and never edits. Both are described in that project's own status file and in `doc/plans/phases/reports/phase-03-delivery.md` in the scratch-app repository.
2. **~~Pull request #2 — the speed check.~~ Merged:** <https://github.com/fvermaut/scratch-app/pull/2>. **The 2-millisecond limit it carries is still a guessed number**, not a measured one — it passes comfortably on this machine but was seen to fail once on a busy one. Merging it did not settle that; it is now a guessed limit living in the codebase rather than in a pull request.
3. **Listen to the app with a screen reader** (VoiceOver + Safari) and confirm the controls are announced sensibly. No automated test can settle this; your own accessibility standard says so outright. The step-by-step script is in `doc/plans/phases/reports/phase-01-verification.md` **in the scratch-app repository, not this one**. Note this is now merged work with one accessibility requirement still unconfirmed.

## Known problems not yet fixed

- ~~**The safety check can still publicly accuse an innocent session — through changes that were never committed.**~~ **The public half is fixed as of 2026-08-14; the wrong-name half is not.** It posted on a ticket that the session had changed a file outside the project it was working on. It had not; **I had**, minutes earlier, and had not committed it. The August fix works by reading a label that every commit carries saying who made it — and **a change sitting uncommitted carries no label at all**, so nothing can tell whose it is. ✏ **On your ruling, the safety checks no longer post on a project's tickets at all** — see the next bullet. So the check can still name the wrong session, but it now says so **to that session, which can answer back**, instead of to a ticket that cannot. **The habit that still helps: commit or stash your own changes before the machine starts working.**
- **✏ New, 2026-08-14 — you decided where these findings go, and both halves changed.** The safety checks had two opposite failures on the same day: one shouted something **wrong** where everybody could see it (the bullet above), and one stated something **true** where nobody did (the working-line bullet further down — six correct warnings into a file nobody reads). Same cause: **where a finding went was decided by who started the session, not by who could do anything about it.** Your two rulings: **the session that caused it hears first** — it gets one round to fix the problem or to say why the finding is wrong, which is a thing it knows and the check cannot — and **nothing is ever posted on a project's ticket again**; what survives that round **flags the job instead**, so it shows up in `timone status`. Written down as `doc/adr/0027-a-guardrail-finding-is-addressed-to-the-session-that-caused-it.md` and **built the same day**, 818 automatic checks passing. **Three costs, named rather than hidden:** one of the twenty-one promises (the safety-check one) **stops counting as kept** until somebody who did not build it watches the new behaviour, because its old wording promised the ticket comment you just removed; a session can now waste a turn arguing with a finding that was right; and **a session that is killed right after being told something reports nothing at all** — that hole is real and written into the decision rather than papered over.
- **A session that has been killed is reported as still working, indefinitely.** After the watcher was stopped on 2026-08-14, `timone status` went on reading *"working on it now … for 7m53s"* about a session that was dead. That is the sleeping-laptop rule being honest — nothing was watching the silence, so nothing may call it dead — but **with no watcher running, nothing ever watches, so the line never corrects itself.** The rule is right while the machine is running and has this hole when it is not.
- ~~**Two finished tickets are still recorded as in progress right now, and one of them is holding the throwaway app.**~~ **Cleared 2026-08-14.** GitHub went down during the proof run, so the watcher never got to see the endings and tick them off — throwaway app #29 read as working at planning and #30 as queued, while both tickets were closed. Both entries are gone from the machine's records. **Clearing it turned up two further problems, and they are the next two bullets**: what a waiting job does when its ticket has closed underneath it, and the fact that there is no way to stop a job at all.
- **A job whose turn comes up on a closed ticket starts anyway, and gets paid for.** The moment the throwaway app was freed on 2026-08-14, the job queued behind it started a session on **a ticket that had been closed for hours**. It was killed before it published anything, but it had already been started and billed. **Nothing re-checks whether a ticket is still open between joining the queue and reaching the front of it** — so on a real project, closing a ticket that is waiting its turn does not stop the machine working on it, and what it produces would land on a closed thread.
- **There is no way to stop a job.** You can restart one and you can hand one over; **nothing cancels one.** Clearing the two stuck entries above meant editing the machine's internal records by hand — which is exactly the thing that was reported as fixed on 8 August. It was fixed for jobs that *should* carry on; a job that should never have existed, or whose ticket was closed underneath it, still has no exit. This and the bullet above close together.
- **A working line meant for a project can get made a second time in Timone's own folder, and then quietly collect Timone's own work.** On 2026-08-14 the throwaway app's job #29 made its working line in the right place — and then, twenty minutes later, **a second copy of the same line appeared here, in Timone's own folder**, made by the step that records your approval. That step's instruction names the line and **never says which folder it lives in**, while every session starts in Timone's root by design. Nothing switched back afterwards, so three hours later the session writing up phases 19 and 20 committed **all seven of its permanent records onto the stray line** — including its own handover, which then stated in writing that they were on the main line. They were not. **Thirteen pieces of work were sitting off the server**, the whole written record of two phases plus the last day's building. ✏ **Sorted on 2026-08-14 on your say-so** — nothing had to be merged and nothing clashed, because the stray line held only Timone's own work; it is deleted and all thirteen are on the server. ✏ **And fixed the same day, on both sides.** Every instruction a session gets now **names the folder its git commands act on**, not just the working line, and says plainly that a plain command at the root hits Timone's own repository instead — including the short approval-recording instruction, which is built separately and had been inheriting none of the shared wording. That is why the shortest instruction in the system was the one that did this. **And a new safety check now names it directly:** a working line belonging to a project, found in Timone's own folder, is reported with whatever work is already stranded on it. Seventeen new automatic checks cover it, one of them replaying the real event against real version control. **Still open, and deliberately not counted as fixed:** the new check writes to the same log nobody reads. **That is the same fault as the false accusation at the top of this list, pointing the other way** — one check shouting something wrong where everybody sees it, another stating something true where nobody does, and **nothing deciding which findings go where.** It is worth settling as one decision rather than two.
- ~~**One written answer sets two sessions going, and you get answered twice**~~ **Fixed and proven 2026-08-14** (found 2026-08-11, reproduced both times it was tried). The full description is at the top of this file. ~~**Until it is fixed, answer the trading app's questions by talking them through rather than writing on the ticket.**~~ The building session had already noticed the underlying oddity and written it down as a curiosity — "the ticket gets read twice per cycle on that path" — without connecting it to a consequence. That was the consequence, and the curiosity was fixed along with it.
- **A watcher that is already running keeps running the code it started with.** Found during the 2026-08-14 proof run, where a watcher up since the afternoon answered a ticket using the old code while the fix sat on disk beside it — and two attempts to start a fresh one exited silently, which read like a different fault and was not. **There is nothing that tells you this**, and no plan to add it yet. **The habit that avoids it: after anything is built, stop the watcher and start it again.**
- **The machine's own log says "resuming" *after* the session has already finished.** The line is printed when the work returns rather than when it starts, so the log reads "done … resolved it" and then "resuming …", which looks exactly like the duplicate-answer fault that was just fixed. Nothing is actually wrong with the behaviour, and **no automatic check can see this** — it is a fault in what it says, not in what it does. It matters because it is the log somebody will read while diagnosing the next problem on this path.
- **A ticket that calls itself a throwaway still gets its decision written into the project's permanent records.** Three fixture questions on the to-do app each said in their own body *"throwaway … not a real product decision"*, and the sessions that resolved them committed and published those decisions into that app's permanent documents anyway — including a feature invented purely to test a lock. Undone on your call on 2026-08-14. ✏ **And it happened again the same day**, on the next proof run: a throwaway question list's specification was written into the to-do app's permanent status file on its main line, so that app's record announced a specification waiting on a decision nobody had asked for. Undone again. **The banner does not bind anything**, and this is exactly how "it's only the test app" turns into real mess in a real repository. **One occurrence was an accident; two in two days is a mechanism**, and nothing yet treats "this is a fixture" as a limit on what a session may commit.
- **A question waiting on you still opens in the wrong voice.** The first sentence you read on any map question is *"Before I write down what … actually needs"* — promising a write-up that this kind of session is explicitly forbidden to produce. Recorded on 2026-08-09, again on 2026-08-11, and again on 2026-08-14 on a third occasion. Cosmetic, wrong, and the first thing a human sees.
- **One automatic check fails now and then, and it is no longer a mystery.** Since 9 August a single test had been failing occasionally with nobody able to say which one it was. It is now named: it does real work with the version-control system and simply runs out of its five-second allowance when the machine is busy running everything else at once. **It was confirmed failing on code written before any of this month's work**, so a red run on that name is not a sign that something broke. Not fixed, because it is a slow test rather than a wrong one.
- **A watcher that is killed outright leaves its project locked for up to two minutes.** Another one started immediately after is refused, because for the first couple of minutes the machine treats a quiet lock as possibly-busy without checking whether the process is still there. **This is a deliberate trade, not a fault** — the alternative makes the check the expensive part of every start — and the cost is named here so it is not met as a surprise: after a crash, wait a couple of minutes or the next start refuses.
- ~~**The run-this-line command refuses a map question that was never connected to the machine**~~ **Built on 2026-08-14 and watched working.** The command now reads any open ticket, works out from the ticket itself what it is, creates the job and picks it up — the refusal is gone from every open ticket. It survives in exactly two places, each with its own sentence: a ticket that **does not exist**, and one that is **closed**. **The remaining gap is narrow and named:** at the proof run the job was created but the conversation did not open, because the project was busy with something else, so the "and then the right conversation opens" half is still unwatched.
- **`timone status` does not understand "waiting on another question".** Nothing reads the "blocked by" line on a ticket — it is prose, and the machine has no notion of blocking. ✏ **Half of what you saw on 2026-08-11 is gone, and half is not.** The *disagreement* — the terminal asking you to answer trading-app #11 while #11 said nothing was needed — cannot happen any more, because since 2026-08-14 both the terminal and the ticket print the same computed sentence; that was watched on the throwaway app, word for word, and **not on the trading app**. What is unchanged is that neither of them knows a ticket can be waiting on another ticket, so they can now be **wrong together** where they used to disagree.
- ~~**Nothing tells a session that just settled a question to go and refresh the questions that were waiting on it.**~~ **Fixed on 2026-08-14, and it is a rule now rather than a session choosing well.** Every minute, the machine works out what each open ticket should say and rewrites the line **only where it differs** — so a ticket whose blocker closed comes up to date because a minute passed, not because anyone remembered it. Watched both ways at the proof run: the list of questions rewrote its own line the moment its last question closed, and a minute over tickets that had not moved rewrote **nothing at all**. ~~It fails silently when a blocker is settled outside the machine~~ — **that hole is closed too**, because nothing depends on a session noticing any more.
- **`timone status` cannot tell a question you have never answered from one you have answered twice and been handed back.** Both read "waiting on you: a conversation in your terminal". The limit that stops the machine badgering you lives in the ticket thread, which is the right place; the status line just cannot see it.
- **"Committed" and "you can actually see it" were not the same thing, anywhere in the process — now fixed** (2026-08-02). The rulebook told every stage to write this status file and commit it, and **never once said to send it anywhere**. Only the delivery stage escaped, and by luck: the command that opens a pull request refuses to work on unsent work, so it was forced to. The failure is silent by nature — nothing breaks locally, the stage looks finished, and the gap shows up only when someone goes looking for work that was reported done. It was seen three times in one day: the feedback stage's first real run wrote four permanent records that existed on one laptop, and both a checking run and a delivery run died in the gap between committing and sending, leaving reports on disk and nothing on the server. The rule now says plainly that **a stage that has committed and not sent has not finished**. **Partly closed on 2026-08-03:** something now checks it — after every session the machine starts by itself, an automatic check looks for committed-but-unsent work and says so loudly on the ticket. It was proven by deliberately leaving a commit unsent and watching it complain, then watching it stay silent on a clean run. **It only covers sessions the machine starts.** When you run a stage yourself in a terminal, nothing checks. **This is what phase 14 fixes, and it stopped being theoretical on 2026-08-06:** the stray commit that blocked a build came from a session you ran by hand. The reason it went unwatched turns out to be small and fixable — the check was wired into the machine's own start-a-session code, so only sessions starting there passed through it. Phase 14 moves it to a place *every* session passes through, yours included, and deletes the old wiring rather than keeping both (two copies of one rule is how the two quietly stop agreeing). **Closed 2026-08-08.** The move is done and was proven live in a single pass: a session you ran by hand committed something stray and the check caught it, printed it to you and wrote it to its own log — and posted nothing on any ticket, which is deliberate, since a session of yours belongs to no ticket. A clean session of yours said nothing at all. The same pass proved the machine's own sessions still behave as before. **One new fault came with it, and it is listed below rather than buried here:** the check can name the wrong session when two are open at once.
- **One of the to-do app's requirements is currently watched by nothing, and this is expected rather than broken.** Its on-screen behaviour rule was reworded on 2026-08-02, which by design drops it out of the automatic safety net until someone re-checks it against the new wording — and that piece of work has not started. The checking pass on 2026-08-02 computed the safety net, noticed the hole, and wrote it into its own report rather than leaving it to be discovered. It is listed here for the same reason: if that queued work slips, this is the thing going unchecked.
- **This file was wrong until today, and that is itself a finding.** While the delivery skill was being proved, the to-do app's status file was updated and this one was not — so for several hours the two disagreed about whether the delivery stage existed at all. The rule says a stage that changes things and leaves the status file stale has not finished. Timone's own work puts documentation last, which is exactly the window where this can happen.
- **The onboarding skill cannot repair a project.** It refuses to run on any project already registered — which is every project that could have a file missing. It is now a tracked requirement rather than a loose note.
- **Nothing detects when the standards library goes out of date.** Five entries were wrong in one weekend, each correct when written, each found only because something finally ran it. Now a tracked requirement — deliberately without a solution attached, because picking a mechanism now would be guessing. It needs an interview session first.
- **Timone's own work is planned by hand.** The planning skill only works on managed projects, by choice. So this file's sections are hand-written, not generated.
- **Every message the machine posts still appears under your name.** The marker line added on 2026-08-03 says plainly that the machine wrote it, and the machine can now tell its own words from yours — but GitHub still shows your face and your username on all of it. The real fix is a separate robot account, which needs a credential from you and is written down as its own piece of work.
- ~~**Two of the three registered projects break every poll.**~~ **Fixed on 2026-08-03.** `scratch-app-2` and `scratch-existing` were local test fixtures, not real GitHub repositories, so the watcher refused them and logged an error every cycle. You chose to drop them from the register rather than teach the watcher to ignore them quietly; their folders are still on disk, and the past write-ups that used them are unchanged. `scratch-app` is now the only registered project.
- **The give-up-and-ask-a-human path has never fired — now true of three skills.** Building, checking and delivering all have a "I've tried and stopped, over to you" route, fully written and never triggered. Same reasoning each time: provoking it means building a trap designed to defeat a competent agent, which tests my ingenuity rather than the tool. The first real failure on real work is its test.
- **The status file was fixed by hand on a live pull request.** The to-do app had no copy on its main line and two divergent copies on its branches — the collision you predicted, which fired the moment the second merge was attempted. All three now agree. **Partly closed on 2026-08-03**, on the same terms as the entry above: an automatic check now catches the status file being written on a working branch — but only for sessions the machine starts by itself, and so far only in tests, since no automatic session has yet written a file at all. **The "only sessions the machine starts" half is what phase 14 closes**, for this check and the other two together. **Closed 2026-08-08** — see the entry above for how it was proven. This particular rule still has not *fired* on a real violation, because none has happened; its red side rests on the tests, which is the acceptable remainder.

- **A machine that gets killed halfway through leaves the job stuck forever — and blocks the project behind it.** If the background service dies mid-work (crash, laptop lid, out of memory), the job it was running stays marked "in progress" with nothing left to progress it. The recovery command refuses it on purpose: as far as the record is concerned, that job is being worked on right now. Worse than the stuck job — a job in that state **holds its project**, so every other ticket on that repository queues behind something that will never finish, and the only way out is hand-editing the machine's internal file. **Phase 14 fixes it**, and the fix comes free with the every-thirty-seconds progress reporting: something that reports every thirty seconds is also something whose silence proves it died. The deliberate limit: a reclaimed job is **stopped and reported, not restarted** — a crash halfway through a build can leave half-finished work behind, and re-running blind inherits it. So an overnight run that crashes waits for you rather than guessing. **Closed 2026-08-08, and better proven than planned.** The test was meant to be a deliberate kill; instead a stray process killed the service for real, mid-build, and the fix handled it exactly as designed — the job was reclaimed on the next cycle, marked stopped with a plain reason, the ticket told, the project freed, and the recovery command named on screen. `timone retry` then picked it up where it stood. **But the reverse case is now a known fault** — see the sleeping-laptop entry below. The stuck-forever problem is gone; the risk has moved to the opposite error, killing a job that is perfectly healthy.
- ~~**The machine is silent while it works, so "busy" and "hung" look identical.**~~ **Fixed 2026-08-08.** It now prints a line every thirty seconds while a session works, and one line saying what it cost when the session ends. The closing cost line is accurate; two of the numbers on the *live* line are not — see the next two entries.
- ~~**Every step of the process uses the same expensive setting.**~~ **Fixed and signed off 2026-08-08.** Each step now runs on a machine chosen for it, and all five were watched doing so on real tickets: sorting on the cheap-and-fast one, writing down requirements and planning and building on the capable one, and recording your approval on the cheapest one of all.
- ~~**It can blame the wrong session for a commit.**~~ **Fixed and signed off 2026-08-08.** The rule now reads the label each commit carries saying which session made it, and ignores commits belonging to someone else. Proved by recreating the exact conditions: the same commit shown to two sessions at once, named by the one that made it and ignored by the one that didn't — then a real mistake made on purpose, to confirm the alarms still go off rather than having been quietly silenced. The public comment on the to-do app's ticket has been corrected in place. **One deliberate limit:** a commit carrying **no** label at all is still attributed to whoever is checking. Nothing can tell who made such a commit, and over-reporting a real mistake is the safer of the two errors. ✏ **2026-08-14: that limit is wider than it read, and it cost a second public false accusation** — a change that has not been committed at all carries no label either, so a file you edit and leave uncommitted while the machine works gets blamed on the machine. See the first entry at the top of this list.
- ~~**Restarting a failed job carries over the old attempt's warnings.**~~ **Fixed 2026-08-08.** A restarted job starts with a clean slate. Warnings it earns *itself* after restarting are kept — only the dead attempt's are dropped.
- **The live "how much has it written" number is too low, by two to six times.** The figure on the every-thirty-seconds line under-counts, always downwards, and worst when the step is spreading work across helpers — but it was also wrong by nearly six times on a step using no helpers at all, so the explanation is not yet complete. **The cost line printed when a session ends is correct** and is the number to trust. **This is now known *not* to be the same fault as the clock below**, so it needs its own investigation rather than riding along on that fix. ✏ **2026-08-14 — caught in the act, and it is better understood than it was.** The number sat frozen at the same value for **four** consecutive half-minute updates while the session was plainly alive and working, and then the closing line printed a figure nearly twice as large. **So it lags behind rather than stops**, and the closing figure remains the one to trust. Still unexplained; still nobody's current job.
- **The live clock is wrong when your laptop sleeps, and that can get healthy work killed. Now understood, not yet fixed.** There are two clocks and they measure different things: the live line counts **wall-clock** time, which keeps running while your laptop sleeps, and the closing cost line counts the time the machine was actually **awake**. Both are right; they were being shown under one name. Your Mac keeps its own sleep log, and it settled this without any experiment — overnight it sleeps in a **15-minute cycle**, waking for about 45 seconds each time. Over the 4 hours 13 minutes that run took, that leaves roughly 19 minutes awake, and the closing line said 19 minutes 30 seconds. **The serious half is unchanged:** this same heartbeat is what proves a job alive, and the sleep log shows **113 stretches of silence longer than the service's patience allows in a single night**. Left running normally it would stop healthy work and throw it away. ~~**Until this is fixed, do not leave the service running unattended overnight on a laptop that sleeps.**~~ **Fixed on 2026-08-08 and proven the same day.** The service now has to have been *awake and watching* before it is allowed to call anything dead — so silence nobody was listening for stops counting as evidence. The clock half is fixed too, and each clock now says which one it is. **The token half — the live count of how much has been written — is not fixed and is not explained**, and it turned out not to share a cause with the clock, which is why it stands as its own entry above rather than a footnote to this one.

~~**This warning survived phase 16 on purpose.**~~ **Phase 17 fixed it on 2026-08-08 and proved the fix by running it.** See the top of this file. The warning is rewritten below rather than deleted, because what replaces it is narrower but not nothing.

> **The new position, in one line: you can now leave it running overnight.** The service will no longer kill healthy work while your laptop sleeps, and that was watched happening — a job frozen for two and a half minutes came back looking dead by every measure the old rule used, and was left alone, with the service printing why.
>
> **One thing was not tested, and I am telling you rather than hoping.** The proof froze the *program* rather than sleeping the *laptop*. Those are the same thing as far as the fix is concerned — the fix reads two timestamps and asks whether it was running in between — and your Mac's own sleep log, measured back in phase 15, already established that real sleep produces exactly this kind of gap. So both halves are measured; neither is assumed. But nobody has yet closed the lid on it and looked in the morning. **If you want that certainty, it costs one lid-close and one glance**, and it is the only thing between "proven" and "proven twice".
>
> **A side effect worth knowing.** While the laptop is asleep it wakes for about 45 seconds every 15 minutes, which is not long enough for the service to be sure of anything — so overnight it will mostly decline to make judgements at all. Healthy work is therefore safe, which is the point; but a job that *genuinely* died at 2am now waits until morning to be cleared up rather than being cleared up at 2:02am. That is the cautious direction and it is deliberate.
>
> **This is the one call I made without you** (you said to run it autonomously). Say the word and the old blanket warning goes back.

## Jargon key

Only if you need it. **Skill** — a written instruction file an agent loads and follows for one stage. **Stage** — one step of the process, from sorting a request through to maintenance. **Phase** — a chunk of work planned and built as one unit; the numbered "phase 07" and "phase 08" in this file are Timone's own chunks of work, in order. **Branch** — a named line of development in Git; **merging** joins one into another, and **`main`** is the primary one. **Pull request** — the page where a branch is reviewed and where you press merge. **Requirement** — a numbered, checkable statement of what something must do; Timone's own are in `doc/specs/prd/prd-01-process-layer.criteria.md`. **Verified** — checked by an agent that did not build the thing, with evidence recorded.
